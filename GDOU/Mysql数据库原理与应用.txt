项目 2 数据库基础应用-基于图形界面项目提出

对初学者来说图形界面是一个非常友好的工具，可以将很多抽象的内容直观的展现出来，很多程序员也喜欢使用这个工具。它可以让我们快速的掌握数据库管理系统的使用，对数据库系统的结构、功能和工作方式有一个全面的理解。对于我们进一步理解项目一中的概念有很大的帮助，也为后续的命令的学习以及更深入的数据库应用和管理技术的学习打下基础。项目分析
本项目将利用 Navicat 图形界面管理工具进行操作，所以我们需要一台已经安装好了MySQL 数据库管理系统的服务，以及已经安装好 Navicat 的终端。
本项目以操作为主，融入的理论内容约占 30%左右。操作内容包括：数据库连接的建立，在 Navicat 当中创建和修改数据库，创建、修改和管理数据表，创建、删除表中的约束以及向表中添加、修改、删除数据等操作。
2.1 任务 1：创建和修改数据库2.1.1 相关知识
1. MySQL 客户端和服务端交互过程
举个例子，客户端发送一条 SQL 命令给数据库服务器。假设客户端使用的字符集为utf8，MySQL 服务端使用的也是 utf8 字符集来编码并存储数据的。交互过程如下：1 首先客户端在连接 MySQL 服务器前（比如：我们使用 Navicat 来连接MySQL），MySQL 客户端会先获取自己操作系统的字符集（此例为 utf8）。然后将字符集、密码、用户名等发送给 MySQL 服务端。
2 MySQL 服务端收到连接请求后，验证用户名、密码等。然后为这个客户端连接建立一个 session 对象，然后将客户端所使用的字符集保存到 session 对象的三个变量上（分别
是：[character_set_client、character_set_connection、character_set_results]上），本案例中三个变量的值都为 utf8。
3 服务器返回信息给客户端确认连接成功。
4 客户端需要向 MySQL 服务端发送一条 SQL 命令。MySQL 客户端首先要对这条SQL
进行编码，按照指定的字符集（此例是 utf8）进行编码，然后发送给MySQL 服务端。5 MySQL 服务端收到 SQL，然后查看该 session 中的 character_set_connection 变量的值（此例是 utf8），对客户端发来的 SQL 进行解码。此时就能正确的解析出客户端发送的SQL 内容了。然后 MySQL 服务端就可以按照 SQL 内容去执行筛选和查询了。6 MySQL 服务端查询完数据后要将数据返回给客户端，此时 MySQL 服务端会先查看[character_set_results]变量的值，得到编码的字符集（utf8），然后使用该字符集对返回数据进行编码。然后发送给客户端。
7 客户端收到数据后，按照操作系统的字符集对服务端响应的数据进行解码。交互完成。
2. 数据库的字符集
我们知道计算机存储数据其实是存储的二进制数据（在底层其实就是一串0、1 组成的字节序列）。mysql 客户端（包括 MySQL 自带的客户端、Navicat、MySQL Workbench、SQLyog等）和服务端通信的时候也需要将字符转化为二进制数据后才能通过网络进行发送，这个过程就称为编码。那么如何编码呢？一个汉字到底要用几个字节来表示呢？到底要使用多少个字节才能表示一个中文符号呢？就需要根据我们具体使用的编码字符集。字符集指的是某个范围字符的编码规则。
MySQL 服务端收到客户端发来的二进制序列后，将这些二进制序列还原为对应的字符，这个过程就称为解码。一般来说编码和解码是成对出现的，有编码就会有解码。假设客户端是按照 utf8 字符集进行的编码，MySQL 服务端也是按照 utf8 字符集进行解码的，这两步没有问题。但如果 MySQL 服务器使用字符集是 ASCII，需要存储的中文字符就会出现乱码。表 2-1 MySQL 常用字符集列表
字符集名称 占用字节数 说明
ASCII 1 收录了 128 个字符，包括空格，标点符号，数字，大小写字母和一些不可见字符，不能表示中文。
gbk 1 ~ 2 收录了汉字（6763 个）以及拉丁字母、希腊字母、日文、俄文等字符，同时这种字符集又兼容 ASCII 字符集。当字符再ASCII
字符集表示范围内的话就采用一个字节表示，否则采用两个字节表示。
utf8 1 ~ 3 在 MySQL 中 utf8 字符集是经过 MySQL 设计者缩减过后的字符集（为了尽可能省空间），该字符集最大使用3 个字节来表示一个字符，不可以存储表情或某些非常特殊的字符，在数据库字符集、表字符集、列字符集等选型的时候需要注意了。utf8mb4 1 ~ 4 MySQL 一般建议使用 utf8mb4 字符集，该字符集最大使用4 个字节来表示一个字符，可以存储表情或某些非常特殊的字符。3. MySQL 的标识符
在 MySQL 数据库中，要访问任何一个对象都要通过其名称来完成，在MySQL 语言中，
对数据库、表、变量、存储过程、函数等的定义和引用都需要通过标识符来完成。我们所说的标识符，实际上就是我们给对象起的名称，本质上是一个字符串。标识符分为常规标识符和分隔标识符两种。
常规标识符：不包含空格，可以不需要使用单引号或方括号将其分隔的标识符。标识符中可以包涵英文字母、数字、汉字、下划线、$和￥，但不能由纯数字组成，最长不超过128个字符，不能和 SQL 语言中的关键字重复。
分隔标识符：是指包含在两个单引号（’’）或者方括号（[ ]）内的字符串，这些字符串中可以包含空格。
2.1.2 任务实施
1. 使用 Navicat for MySQL 创建数据库
打开 Navicat for MySQL 窗体，首先连接到 MySQL 数据库服务器。双击左侧“连接”栏内新建立的连接，就会出现当前服务器中所有的数据库。右键点击该连接或连接栏目中的任何地方，在弹出的菜单中选择“新建数据库”命令，如图 2-2 所示。
图 2-2 使用 nnavicat for mysql 建立数据库窗口 1
在打开的“新建数据库”对话框中，输入数据库名、字符集和排列规则，如果不知道字符集是什么类型的，建议选择“Default character set”，如图 2-2 所示。在MySQL 中，我们也可以直接使用汉字给数据库命名，但建议使用英文或拼音缩写作为数据库的文件名，方便编码处理，我们给数据库命的名字应该便于理解和记忆。
输入完毕以后，点击“确定”按钮，完成新数据库的创建。
图 2-3 使用 nnavicat for mysql 建立数据库窗口 2
要注意的是，用户开始创建的数据库是空的，也就是说没有数据。数据库创建位置是安装时设定的，默认在“C:/ProgramData/MySQL/MySQL Server 8.0/Data”文件夹下。一般来说，每个数据库对应一个文件夹，文件夹内有一到多个文件。
2. 数据库的其他操作
在 MySQL 中对数据库进行修改操作主要是修改数据库的名称、字符集和校对规则。其中数据库名称的修改与存储引擎有关，如果是系统默认的 InnoDB 存储引擎是不能修改数据库名称的。
1 在 Navicat for MySQL 窗体中，右键点击数据库，在弹出的菜单中选择“数据库属性” 命令，如图 2-4 所示，其中数据库的名称是灰色的不可修改模式，但字符集和排序规则可以修改。
图 2-4 数据库属性窗口
2 删除数据库是指在服务器中删除已经存在的数据库，删除成功后，数据库中的数据将全部删除，分配的存储空间也将被回收。
在 Navicat for MySQL 窗体中，右键点击要删除的数据库名称，在弹出的菜单中选择“删除数据库”命令，如图 2-5 所示，会弹出“确认删除”对话框，单击“删除”命令按钮将删除此数
据库，单击“取消”命令按钮则不删除数据库返回窗口。
图 2-5 删除数据库命令
2.2 任务 2：数据表管理
2.2.1 相关知识
1. 数据类型
MySQL 数据库中有许多数据库对象，但真正用于保存数据的对象是表。事实上，任何数据操作最终都将转化为对表的操作。表是整个数据库系统的基础，也是数据库中最重要的数据对象。
表定义的是列的集合，每一行代表一条记录，每一列代表一个属性，称为字段。例如表2-2，记录着教工的基本信息，表中每一行代表一个教职工的具体信息，而每一列是教工某一个方面的信息，例如教工号、姓名、性别等。
表 2-2 教工信息表
教工号 姓名 性别 部门 职称 年龄1 张平乐 男 教务处 副研究员 52
2 李明明 男 科研处 研究员 48
3 彭小玲 女 计算机系 副教授 46
4 陈东浩 男 经管系 讲师 38
5 吴恒 男 外语系 副教授 40
6 刘涵珏 女 外语系 讲师 28
7 李先锋 男 工程系 讲师 30
8 陈忠实 女 计算机系 助教 26
（1）数据表的分类
按照表的用途来分类，可以分为系统表和用户表两类。
系统表是维护 MySQL 服务器和数据库正常工作的数据表，每个数据库下都会建立一些系统表，用户一般不要对系统表进行修改等操作，而是由 DBMS 系统自行维护。在前面我们已经知道，安装好 MySQL 后，会自动创建系统数据库，在这些系统数据库中的表绝大部分都是系统表。
用户表是指在用户自定义的数据表。
（2）用户表的主要内容
用户表是由用户建立的、用于某种实际用途的表。
我们根据实际需要设计数据库时，应该考虑好需要什么样的表，各表中应该保存哪些数据及表之间的关系等内容。在创建表时最有效的方法是将用户表中所需信息一次定义完成，包括一些约束条件（后面介绍）和附加信息等。
用户表的主要内容有：
1 表的名字，每个表都需要一个好读好记忆的名字；
2 表所包含的基本数据类型及自定义的数据类型；
3 表的各列的名字及每一列的数据类型（有必要的话还需说明列的宽度等信息）；4 表的主码和外码信息；
5 表中哪些列允许空值；
6 哪些列需要索引以及索引的类型；
7 是否要使用以及何时使用约束、默认设置；
用户表的内容很多，我们在下面的内容中首先说明建立用户表框架的基本信息，再逐步完善用户表，建立表间的关联，然后再往表中添加数据，对数据进行必要的维护操作，如插入、修改和删除等。
（3）表中的列
我们即将介绍数据库中表的建立方法，实际上表的建立就是对表中列的定义，需要为每一列定义列名（字段名）、说明列中的数据的取值范围（数据类型）以及一些其它的内容，比如是否允许为空（Null）等。
表中每一列需要定义列名，即字段名，这些名称需要在建立表时定义，名称要符合标识符的命名规则。
字段的数据类型规定了该列的取值范围和列中数据能够进行的运算。例如姓名应该是字符型的，而年龄应该是数值型的等。
表中的列还需要说明列中的值是否为空（Null），如果不能为空，则这列的数据是必填内容，不能为空。数据表中的内容尽量不要为空，这样计算或统计的结果就不会有误。（4）数据类型
数据库中的数据类型，包括基本数据类型、复合数据类型、序列号类型及几何类型。基本数据类型又包括数值类型、字符类型、二进制类型、日期、时间类型、布尔类型、枚举类型等。
基本数据类型是数据库内置的数据类型，包括 INTEGER、CHAR、VARCHAR 数据类型。1 整数数据类型是最常用的数据类型之一。不同的数据库产品在具体的数据定义上略有不同。如，MySQL 支持的整数又分为 TINYINT 、SMALLINT 、MEDIUMINT、INT 和BIGINT五种，如表 2-3 所示。
表 2-3 MySQL 整数数据类型
类型名称 字节数 无符号数的取值范围 有符号数的取值范围TINYINT 1 0~255 -128~127
SMALLINT 2 0~65535 -32768~32767
MEDIUMINT 3 0~2
22-1 -2
23~2
22-1
INT 4 0~2
32
-1 -2
31~2
31
-1
BIGINT 8 0~2
62-1 -2
63~2
62-1
2 在数据库中，存储的小数都是使用浮点数和定点数来表示的。浮点数的类型单精度浮点数类型（FLOAT）和双精度浮点数类型（DOUBLE）。而定点数类型有DECIMAL、NUMERIC类型。浮点类型中的 FLOAT 和 DOUBLE 是不准确的牺牲精度的数字类型。“不准确”意味着一些数值不能准确的转换成内部格式，并且是以近似的形式存储的，因此存储后再把数据
输出可能会有一些缺失，所以在金融计算等对精度有严格要求的应用中，数据类型应当首选 DECIMAL、NUMERIC 这种精度数据类型。
MySQL 的小数类型如表 2-4 所示。
表 2-4 浮点数和定点数类型
类型名称 字节数 有符号的取值范围 无符号的取值范围FLOAT 4 -3.40E+38～-1.17E -38 0 和 1.17 E -38~3.40E +38
DOUBLE 8 -1.79E+308～2.22E - 308 0 和 2.22E-308~1.79E+308
DECIMAL(M,D) M+2 -1.79E+308～2.22E - 308 0 和 2.22E-308~1.79E+308
需要注意的是 DECIMAL 类型的有效取值范围是由 M 和 D 决定的，其中，M表示的是数据的长度，D 表示的是小数点后的长度。比如，将数据类型为 DECIMAL（6,2）的数据3.1415插入数据库后，显示的结果为 3.14。
3 二进制数据类型用于存储二进制数据，又分为 BINARY、VARBINARY 和BOLB 三种，如表 2-5 所示。
表 2-5 二进制数据类型
类型名称 取值范围和说明
BINARY BINARY（n），表示定长的二进制数据，n 为长度，占n+4 个字节。如果数据长度不足 n，将在数据的后面用“\0”补齐。VARBINARY VARBINARY（n），表示变长的二进制数据，n 为长度，占实际数据长度+4 个字节。
BOLB 可以用来存储数据量很大的二进制数据，如图片、PDF 文档等，根据二进制编码进行比较和排序。4 字符串类型是使用最多的数据类型，可以用它来存储各种字母、数字符号、特殊符号。一般情况下，使用字符类型数据时须在其前后加上单引号。字符数据类型又细分为CHAR和 VARCHAR 两种，如表 2-6 所示。
表 2-6 字符串类型
类型名称 取值范围和说明
CHAR 定义形式为：char[(n)]，表示长度为 n 个字节的固定长度且非Unicode 的字符数据，n 必须是一个介于 1 和8000 之间的数值，存储大小为 n 个字节。
VARCHAR 定义形式为：varchar[(n)]，表示长度为 n 个字节的可变长度且非 Unicode 的字符数据。n 必须是一个介于 1 和8000 之间的数值。存储大小为输入数据的字节的实际长度+1。5 大文本数据类型用于存储大量的字符，例如文章内容、评论等。TEXT 类型分为四种，如表 2-7 所示。
表 2-7 文本数据类型
类型名称 取值范围
TINYTEXT 0~255 字节
TEXT 0~65535 字节
MEDIUMTEXT 0~16777215 字节
LONGTEXT 0~4294967295 字节
6 日期和时间数据类型用于存储日期和时间，又细分为 YEAR、DATE、TIME、DATETIME和 TIMESTAMP 五种，如表 2-8 所示。
表 2-8 日期和时间数据类型
类型名称 字节数 取值范围 日期格式YEAR 1 1901~2155 YYYY
DATE 4 1000-01-01~9999-12-31 YYYY-MM-DD
TIME 3 -838:59:59~838:59:59 HH:MM:SS
DATETIME 8 1000-01-01 00:00:00
~9999-12-31 23:59:59
YYYY-MM-DD
HH:MM:SS
TIMESTAMP 4 1970-01-01 00:00:01
~2038-01-19 03:14:07
YYYY-MM-DD
HH:MM:SS
7 其它数据类型：除上面介绍的数据类型之外还有 ENUM、SET、BIT 三种数据类型，其中 ENUM 类型又称为枚举类型，定义 ENUM 类型的数据格式如下所示：ENUM('值 1','值 2','值 3',...,'值 n')
在上述格式中，('值 1','值 2','值 3',...,'值 n')称为枚举列表，ENUM 类型的数据只能从枚举列表中取，并且只能取一个。需要注意的是，枚举列表中的每个值都有一个顺序编号，MySQL 中存入的就是这个顺序编号，而不是列表中的值。
SET 类型用于表示字符串对象，它的值可以有零个或多个，SET 类型数据的定义格式与ENUM 类型类似，具体语法格式如下所示：
SET('值 1','值 2','值 3',...,'值 n'）
与 ENUM 类型相同，（'值 1','值 2','值 3',...,'值 n'）列表中的每个值有一个顺序编号，MySQL 中存入的也是这个顺序编号，而不是列表中的值。
BIT 类型用于表示二进制数据。定义 BIT 类型的基本语法格式如下所示：BIT(M)
在上述格式中，M 用于表示每个值的位数，范围为 1~64。需要注意的是，如果分配的BIT(M)类型的数据长度小于 M，将在数据的左边用 0 补齐。例如，为BIT(6)分配值b'110'的效果与分配 b'000110'相同。
（5）NULL 的含义
在现实世界中我们填写某些表格时，某些表项的内容不能确定或者没有必要说明时可以不用填写。在 MySQL 中，我们用 NULL 表示数值未知的空值。要注意的是，空值不是“空白”或者“0”，没有两个空值是相等的，当把两个空值进行比较或将空值与任意数值进行比较时均返回未知的空值 NULL。因此，NULL 表示未知、不可用或将在以后添加的数据。在 MySQL 设计表的各个字段（列）时，我们应该尽量避免 NULL 值，因为在数据统计等操作时有 NULL 数据的列可能会出错。我们用 NOT NULL 表示数据列不允许空值，这样就可以确保数据列必须包含有意义的数据，从而确保数据的完整性。
2. 数据库存储引擎
什么是存储引擎呢？简单来说，存储引擎就是如何存储数据，如何为存储的数据建立索引和如何更新、查询数据等技术的实现方法。由于在关系型数据库中数据的存储是以表的形式存储的，所以存储引擎也称为表类型，即存储和操作表的类型。
在Oracle和SQL Server等数据库中只有一种存储引擎，所有数据存储管理方法是一致的。但 MySQL 提供了多种存储引擎，如 MyISAM、InnoDB、MEMORY 等，在MySQL 中使用者可
以根据数据量的大小及不同用途而选择不同的存储引擎，当然如果不特别指定，就使用系统默认的存储引擎。
在 MySQL 中，可以通过在命令行窗口中输入“SHOW ENGINES”命令查看数据库支持的存储引擎。
（1）存储引擎 InnoDB
InnoDB 是当前 MySQL 数据库版本的默认存储引擎，与传统的 ISAM 和MyISAM相比，其最大的特色是支持 ACID 兼容的事务功能。
InnoDB 存储引擎给 MySQL 数据库提供了具有事务、回滚和崩溃修复能力、多版本并发控制的事务安全型表。同时 Innodb 存储引擎还提供外键、行锁、非锁定读(默认情况下读取不会产生锁)、从 4.1 版开始支持每个 Innodb 引擎的表单独放到一个表空间里。Innodb 通过使用MVCC来获取高并发性，并且实现sql标准的4种隔离级别，同时使用一种被称成next-keylocking 的策略来避免幻读(phantom)现象。除此之外 Innodb 引擎还提供了插入缓存(insert
buffer)、二次写(double write)、自适应哈希索引(adaptive hash index)、预读(read ahead)等高性能技术。但其缺点是读写效率稍差，占用的数据空间相对较大。
（2）存储引擎 MyISAM 及其他存储引擎简介
MyISAM 存储引擎的特点是不支持事务，适合 OLAP 应用，即一般使用MyISAM存储引擎管理非事务表。它提供高速存储和检索，以及全文搜索能力。5.0 版本之前，MyISAM默认支持的表大小为 4G，从 5.0 以后，MyISAM 默认支持 256T 的表单数据，且MyISAM只缓存索引数据。
MRG_MyISAM 存储引擎,也被认识为 MERGE 引擎，允许 MySQL DBA 或开发人员将一系列等同的 MyISAM 表以逻辑方式组合在一起，并作为一个对象引用它们，对于诸如数据仓储等 VLDB 环境十分适合。
（3）MOMERY 存储引擎
MOMERY 存储引擎将数据放到内存中，默认使用 hash 索引，不支持text 和blob 类型，varchara 是按照 char 的方式来存储的。数据库使用 MOMERY 存储引擎作为临时表还存储中间结果集(intermediate result)，如果中间集结果大于 MOMERY 表的容量设置，又或者中间结果集包含 text 和 blog 列类型字段，则 MySQL 会把他们转换到 MyISAM 存储引擎表而放到磁盘上，会对查询产生性能影响。
（4）ARCHIVE 存储引擎
ARCHIVE 存储引擎被用于以非常小的覆盖区存储大量无索引数据，这个存储引擎有高效的插入速度，但对查询的支持相对较差。所以说 ARCHIVE 存储引擎的压缩能力较强，主要用于归档存储。为大量很少引用的历史、归档、或安全审计信息的存储和检索提供了完美的解决方案
（5）BLACKHOLE 存储引擎
BLACKHOLE 存储引擎也称为黑洞引擎，它能够接受数据但并不存储数据，即使用该存储引擎存储的数据都会丢失，并且检索总是返回一个空集。
（6）CSV 存储引擎
CSV 存储引擎把数据以逗号分隔的格式存储在文本文件中。CSV 是逻辑上由逗号分隔数据的存储引擎，它会在数据库子目录里为每个文件数据表创建一个后缀名为“.csv”的文件，这是一种普通的文本文件，每个数据行占用一个文件行，而且 CSV 存储引擎不支持索引。要注意的是，对于 MySQL 来说，整个服务器或数据库方案，用户并不一定要使用相同的存储引擎，可以为方案中的每个表使用不同的存储引擎，这是 MySQL 数据库的重要特点。
2.2.2 任务实施
我们在“学生成绩管理”数据库下创建三个用户表，分别是学生表、教师表、课程表和成绩表。三个表的基本内容如下表 2-9、表 2-10、表 2-11 和表 2-12 所示。表 2-9 学生表
列名 类型 长度 约束 说明sno char 12 PRIMARY KEY（主键）学号sname char 8 NOT NULL 姓名xb enum 2 DEFAULT '男' 性别zhy varchar 30 专业in_year year 4 入学年份dept varchar 30 所在系表 2-10 教师表
列名 类型 长度 说明 说明tno char 10 PRIMARY KEY（主键）教工号tname char 8 NOT NULL 教师名txb char 2 DEFAULT '男' 性别zc varchar 20 职称age smallint 年龄表 2-11 课程表
列名 类型 长度 约束 说明cno int 11 PRIMARY KEY（主键）课程号cname varchar 30 NOT NULL 课程名xf smallint 0 NOT NULL 学分tno char 8 任课教师表 2-12 选课成绩表
列名 类型 长度 说明 说明sno char 12 PRIMARY KEY（主键）学号cno int 11 课程号cj int 11 成绩xq char 2 修课学期1. 创建用户表过程说明
下面介绍使用“Navicat for MySQL”工具交互式创建用户表的步骤(1) 启动“Navicat for MySQL”，双击连接名“localhost_3306”建立连接，在“连接”面板中双击“学生成绩管理”数据库，鼠标右键单击其下的“表”节点，在打开的菜单中选择“新建表” 菜单项，如图 2-6 所示。打开“新建表”面板。
图 2-6 选择“新建表”命令 图 2-7 “新建表”面板
(2) 在打开的“新建表”面板中，在其“栏位”选项卡的“名”列下输入表中各字段列的名称，在“类型”列下拉列表框中选择字段列对应的数据类型，在“长度”列下输入字段的长度，在“小数点”列指定小数的位数，在“允许空值(Null)”列下单击复选框以确定该列中的数据是否允许为空值 NULL，如图 2-9 所示。
图 2-8 设置主键 图 2-9 定义“学生表”
(3) 依次定义表中各列，定义完毕后可为表指定主键。选择表的某一列（复合主键按顺序依次操作），单击鼠标右键，在打开的菜单中选择“主键”命令，如图2-8 所示。也可使用工具栏中的主键工具“ ”来设置表的主键。“学生表”的所有列定义完毕后，如图2-9 所示。
(4) 表的所有列定义完毕后可在表的属性面板中定义表的名称和所有者，也可单击工具栏中的“保存”工具，在弹出的“表名”对话框中输入表名称“学生表”后单击“确定”按钮，即保存表的设置，如图 2-10 所示。
图 2-10 “表名”对话框
请同学们依照上面介绍的方法建立“教师表”、“课程表”和“选课成绩表”，其定义分别如下图 2-11、2-12、2-13 所示。
这里设置列的其他属性这里设置列的相关内容
图 2-11 “教师表”定义
图 2-12 “课程表”定义
图 2-13 “选课成绩表”定义
2. 表中设置自动递增列
MySQL 数据表中的自动递增列相当于 Access 数据表中的自动增长列。MySQL 数据表中自动递增列的设置是在表的定义时完成的，如果表中某列要定义为自动递增列，则这个列（字段）的数据类型必须是整数型的，一般设置为整型（int）或大整型（bigint），而不能是字符类型或其它类型。
默认情况下，如果某列设置为自动递增列，则该字段的值从 1 开始自增。下面介绍标识列的设置方法。
对于前面介绍的表 2-11“课程表”，其中的“cno”字段可以定义为自动增加列。(1) 在教工信息表的设计表状态下选中“课程号”列（字段）；
(2) 勾选其下的“自动递增”复选框，如图 2-14 所示；
(3) 设置完成后单击“保存”按钮完成设置。
设置自动递增列
图 2-14 自动递增列的设置
标识列一般可作为主键使用，因为在同一个表中不会有相同的两个标识值存在；使用时标识列也不需要用户输入具体的值，是由系统根据标识种子和标识增量来自动产生。3. 修改用户表结构
已经建立的用户表，如果发现不符合要求，例如某列的类型不合适、列的长度需要增大或缩小、需要增加列或修改列的约束，还有某些列不再需要，想要删除等，这些都可以进行修改。我们这一节主要介绍用户表结构的修改。
(1) 在“连接”面板中双击“学生成绩管理”数据库，展开“表”节点，鼠标右键单击其下需要修改结构的表名“学生表”，在打开的菜单中选择“设计表”菜单项，如图2-15 所示。图 2-15 选择“设计表”命令 图 2-16 “学生表设计”对话框
(2) 在随后打开的学生表设计内容窗格中，可对表的各列的列名、数据类型、是否允许为空和其它属性进行修改，如图 2-16 所示；
(3) 如果想往表中插入新的列，可在学生表设计内容窗格中单击命令，如图2-17所示，然后输入列名，定义列的类型、相关属性和列是否设置为空等内容；如果想删除某列，在选中的列上单击 命令，然后在弹出的“确认删除”对话框中选择“删除”命令即可，如图 2-18 所示。
图 2-17 添加栏位
2-18 确认删除栏位对话框
(4) 全部修改完成后，单击表设计内容窗格中的“ ”按钮，完成表结构的修改。修改表的结构很简单，但一般应该在往表中输入数据前修改表的结构，否则会影响现有
数据的存储或违反相应约束规定而导致现有数据的损坏。
4. 删除用户表
如果数据库中有不需要的数据表，可以将其删除，以便释放其所占有的空间。但删除的表是不能再恢复的，所以在删除表时一定要小心确认。
下面我们介绍使用“Navicat for MySQL”工具删除用户表的基本方法。假设我们已经在“学生成绩管理”数据库中建立了一个表“学生表备份”，这个表不再需要，想从数据库中删除它，其方法如下。
(1) 启动“Navicat for MySQL”， 双击连接名“localhost_3306”建立连接，在“连接”面板中双击“学生成绩管理”数据库，展开“表”节点，鼠标右键单击其下的“学生表_copy”表节点，在打开的菜单中选择“删除表”菜单命令。
(2) 如果表建立有外码，则必须首先删除外码，然后才能删除表。对于已删除的用户表，在“Navicat for MySQL”，工具中不再存在。另外请注意：不能删除当前正在使用的表；也不要试图删除系统表。
2.3 任务 3：数据约束的使用2.3.1 相关知识
数据库中的数据（存放在表中）应该防止输入不符合语义的错误数据，而始终保持其中数据的正确性、一致性和有效性，这就是我们这节要介绍的数据完整性。数据完整性是衡量数据库质量好坏的标准之一，MySQL 数据库提供了完善的数据完整性机制。我们这一节首先介绍数据完整性的基本概念，然后用实例说明在 MySQL 中约束和默认对象等的创建和管理。
1. 数据完整性
数据完整性是指数据的正确性、一致性和有效性，是指数据库中不应该存在不符合语义的数据。所谓正确性是指数据表中的数据应该是正确的，比如学生选修某门课程的成绩应该是 85 分，但不小心输入成 185 分，数据库管理系统应该能够检测出来并指出错误。一致性是指数据库中各个表中的数据应该相互照应的，比如学生表中一个学生的学号和这个学生在成绩表中的学号应该对应一致，在学生表中不存在的学号在成绩表中不应该有相应的选课记录等；而有效性是指数据应该是合法有效的，比如学生的性别应该是“男”或“女”，而不能是其它值。
数据完整性可分为四种类型：实体完整性、域完整性、参照完整性和用户定义完整性。实体完整性是指任何一个实体（对应表中的一行或一条记录）都有区别于其它实体的特征。比如世界上没有完全相同的两个人（实体），对应到数据表中每个人对应一条记录其中的编号（或者说身份证号）应该是不同的。
域完整性是指表中每列的数据应该具有正确的数据类型、格式和有效的数值范围。参照完整性是指在两个表的主键和外键之间数据的一致。其含义在三：一是保证被参照表和参照表之间数据的一致；二是防止数据丢失或者无意义的数据；最后可以禁止在从表（例如“学生成绩管理”数据库中的“成绩表”，其“学号”列是“学生表”的外码）中插入被参照表（“学生成绩管理”数据库中的“学生表”）中不存在的关键字的记录。
用户定义完整性是用户希望定义的数据的完整性。例如电话号码是8 位的数字码，邮政
编号为 6 位数字码，对学生出生日期范围的限制等。
以 MySQL 为例，用来实施数据完整性的途径主要有创建表时的列级约束或表级约束、默认（Default）、触发器（Trigger）、数据类型（Data Type）、索引（Index）和存储过程（StoredProcedure）等，表 2-13 中列出了 MySQL 实施完整性的主要途径。
表 2-13 MySQL 实施完整性的主要途径
数据完整性类型 实施途径
实体完整性
Primary Key（主键）
UNIQUE 约束
索引
域完整性
Default（默认）
Foreign Key（外键）
Data Type（数据类型）
NOT NULL （非空）
参照完整性
Foreign Key（外键）
Trigger（触发器）
Stored Procedure（存储过程）用户定义的完整性
列级约束和表级约束（Create Table）Trigger（触发器）
Stored Procedure（存储过程）2. MySQL 的约束类型
约束（Constraint）定义了列允许的取值，是强制完整性的标准机制，常见的约束机制包括以下几种。
(1) NOT NULL 约束（非空约束）：指定数据列不接受空值（NULL）。(2) PRIMARY KEY 约束（主键约束）：列或列的组合，在一个表中不允许有两行记录包含同样的主键值。
(3) UNIQUE 约束（惟一约束）：在列或列的组合内强制执行值的惟一性。(4) FOREIGN KEY 约束（外键约束）：一个表的外键指向另一个表的主键，当一个外键没有与之对应的主键值时，系统可阻止其插入或修改。
(5) 默认值约束：当不给一个记录中的某列分量输入值时则采用由<常量表达式>所提供的值，整数、浮点数、用单引号括起来的字符串或日期都是常量表达式。2.3.2 任务实施
1. 创建非空约束
在“Navicat for MySQL”工具中创建表时，在某个字段列定义时，在其“允许空值（null）” 列下的复选框去掉“√”，即建立了列的“非空约束”，如图 2-19 所示。
非空约束图 2-19 创建表时建立非空约束
2. 创建 PRIMARY KEY 约束（主键约束）
主键是能够唯一标识表中每个记录的字段或若干字段的组合。主键分为单个字段的主键和多个字段的组合主键两种情况。主键约束是用来实现实体完整性的约束，表中定义了主键后，就不允许有相同主键的两个记录存在。
在“Navicat for MySQL”工具中创建表时，选中要设为主键的列或列的组合（如果是多个列组合为主键，按顺序为每个列添加主键），单击鼠标右键，在弹击的菜单中选择“主键” 命令，如图 2-20 所示。也可单击工具栏中的钥匙形状的主键工具来设置表的主键。主键约束
图 2-20 定义表的主键
上面的操作完成之后，有个问题请大家结合主键约束的概念进行思考：单个字段的主键约束与多个字段组合的主键约束有什么区别？
3. 创建 UNIQUE 约束（惟一约束）
有需要时可以为非主键列创建惟一约束，以确定指定列不允许有相同的值。创建惟一约束时自动创建索引。
在“Navicat for MySQL”工具中创建表时，选中“索引”选项卡来进行设置。在索引内容栏中单击“名”文本框，输入索引的名称。选中“栏位”文本框，单击右边的按钮弹出“选择栏位”对话框如图 2-21 所示，选择要创建唯一约束的列，单击“确定”按钮。在索引类型的下拉列中选择“Unique”，在索引方法的下拉列中选择“BTREE”，然后单击“保存”按钮，如图 2-22 所示。
选择 cname 列
图 2-21 “选择栏位”对话框
索引选项卡
图 2-22 “索引”对话框
4. 创建 FOREIGN KEY 约束（外键约束）
在“Navicat for MySQL”工具中创建外键约束的方法如下：
在表设计中单击 “外键”选项卡，打开“外键”对话框，在其中显示已有的外键关系，单击其中的“添加外键”命令按钮添加新的外键关系，单击“删除外键”按钮可删除已建立的选中的外键关系。
“外键”选项卡
图 2-23 “外键”对话框
在“外键”对话框中首先定义外键名，选中“栏位”文本框，单击右边的按钮弹出“选择栏位”对话框，选择要创建外键约束的列，单击“确定”按钮。在参考数据库的下拉列中选择当前数据库名，被参考表的下拉列中设定主键表，选中“参考栏位”文本框，单击右边的按钮弹出“选择栏位”对话框，选择主键列的名字，单击“确定”按钮。在删除时的下拉列中选择“RESTRICT”，在更新时的下拉列中选择“RESTRICT”，然后单击“保存”按钮，即建立了外键约束，如图 2-23 所示。
5. 创建默认值约束
在 MySQL 中，建立用户表时可定义列的默认值。例如，将“课程表”的“xf”列（表示学分数量）定义默认值为“3”。创建默认值约束方法如下： ① 在课程表设计窗格中单击选择“xf”列；
② 在其下面的“属性”窗格中的“默认”栏中输入字符‘3’，如图 2-24 所示；③ 关闭表设计窗口，保存设置即可。
选择 xf 列
输入默认值
图 2-24 “默认约束”对话框
6. 查看表中约束名称
如果要查看表中定义的主键、外键、唯一、非空和默认约束，在打开的Navicat for MySQL主界面中选中要查看的表对象，然后点击 ，即可打开设计表视图查看表中的各种约束及其名称。
请注意，一定要在表的设计状态才能点击选项卡上的对应命令按钮，打开对应的对话框进行查看。
7. 建立用户表之间的关系图
数据库中的用户表往往不是孤立的，而是相互关联的。外码约束就是表之间的列建立的一种联系，以保持数据之间的一致性或者说参照性。
数据库有一个称为“模型”的逻辑对象，其作用是以图形方式显示通过数据连接选择的表或表结构化对象，同时也显示它们之间的联接关系。使用“Navicat for MySQL”工具建立关系图的方法：
(1) 启动“Navicat for MySQL”，双击连接名“localhost_3306”建立连接，在“连接”面板中双击“学生成绩管理”数据库，鼠标右键单击“学生成绩管理”数据库，在打开的菜单中选择“逆向数据库到模型”菜单项，如图 2-25 所示，生成的模型如图 2-26 所示。
图 2-25 “逆向数据库到模型”菜单命令
图 2-26 “学生成绩管理”关系模型
(2) 数据模型生成后，就单击“模型”对话框的“保存”按钮，弹出如图2-27 所示的“模型名”对话框，在其中输入数据模型的名称，单击“确定”按钮，完成数据库模型的建立。图 2-27 “模型名”对话框
数据库模型建立好后，数据库的表间约束将防止一些不正确数据的插入。比如在学生成绩管理数据库中成绩表中的学号和课程号必须是学生表和课程表中已经存在的学号和课程号。
建立数据库关系图一般应该在往表中输入数据之前建立，否则如果数据表中有数据违反外码约束将会出现错误，导致关系图创建失败。
2.4 任务 4：图形界面下管理数据2.4.1 相关知识
数据库对象有表、约束、索引、视图、ER 图表、用户、触发器、存储过程、函数等。1. 表格
数据库中的表与我们日常生活中使用的表格类似，它也是由行（Row）和列（Column）组成的。列由同类的信息组成，每列又称为一个字段，每列的标题称为字段名。行包括了若干列信息项。一行数据称为一个或一条记录，它表达有一定意义的信息组合。一个数据库表由一条或多条记录组成，没有记录的表称为空表。每个表中通常都有一个主关键字，用于唯一地确定一条记录。
2. 索引
索引是根据指定的数据库表列建立起来的顺序。它提供了快速访问数据的途径，并且可监督表的数据，使其索引所指向的列中的数据不重复，如主键索引、唯一索引。3. 视图
视图看上去同表似乎一模一样，具有一组命名的字段和数据项，但它其实是一个虚拟的表，在数据库中并不实际存在。视图是由查询数据库表产生的，它限制了用户能看到和修改的数据。由此可见，视图可以用来控制用户对数据的访问，并能简化数据的显示，即通过视图只显示那些需要的数据信息。
4. ER 图表
图表其实就是数据库表之间的关系示意图。利用它可以编辑表与表之间的关系。5. 用户
所谓用户就是有权限访问数据库的人。
同时需要自己登陆账号和密码。用户分为：管理员用户和普通用户。前者可对数据库进行修改删除，后者只能进行阅读查看等操作。
6. 触发器
触发器由事件来触发，可以查询其他表，而且可以包含复杂的 SQL 语句。它们主要用于强制服从复杂的业务规则或要求。也可用于强制引用完整性，以便在多个表中添加、更新或删除行时，保留在这些表之间所定义的关系。
7. 存储过程
存储过程是为完成特定的功能而汇集在一起的一组 SQL 程序语句，经编译后存储在数据库中的 SQL 程序。
8. 函数
函数与过程很类似，一般用于计算数据，声明为 FUNCTION,需要描述返回类型，且PL/SQL
块中至少有一个有效的 RETURN 语句；函数不能独立运行，必须作为表达式的一部分；在
DML 和 DQL 中可调用函数。
函数的目标是返回一个值。大多数函数都返回一个标量值(scalar value)，标量值代表一个数据单元或一个简单值。实际上，函数可以返回任何数据类型，包括表、游标等可返回完整的多行结果集的类型。
2.4.1 任务实施
创建好用户表结构之后，表内是空的，没有数据
记录。我们在这一任务中介绍在已经建立好表结构的
表中如何插入、修改、删除和查看数据记录。
1. 在 Navicat for MySQL 中插入记录
启 动 “Navicat for MySQL” ， 双 击 连 接 名
“localhost_3306”建立连接，在“连接”面板中双击“学生
成绩管理”数据库，展开“表”节点，鼠标右键单击其下
需要插入记录的表名“学生表”，在打开的菜单中选择
“打开表”菜单项，如图 2-28 所示。在这里可以输入新
的记录内容，如图 2-29 所示，但输入新的记录内容时
要注意以下几点。
在这里插入新记录
图 2-29 在编辑状态下插入新记录
(1) 输入记录中的每个字段内容时，要注意与表定义时该字段的类型相符、长度和精度等相符，否则会出现警告框，且整个记录无效，不能保存到数据表中；(2) 如果定义表时某列的值不能为空（NULL），则必须要输入内容，否则也会出现警告框，告诉你表的哪列数据不能为空；
(3) 如果字段是外键，则在主表中不存在的值不能在外键表中出现，否则也会出现警告信息，记录插入无效；
(4) 如果表中某列已经定义了默认值，在输入记录时想使用默认值则不要在对应的列内输入任何内容，保存这条记录时这个列自动取你设定的默认值；
(5) 在输入完一条记录后，单击下方的 按钮插入下一条记录，数据插入完成后单击下方的 按钮保存符合条件的记录。
2. 在 Navicat for MySQL 中更新记录
在打开的“学生表”结果窗体中用鼠标单击要更新的记录行中某个字段单元格，即可以编辑此单元格的内容，修改一个字段的值后可以继续修改本记录的其它字段值，当一行的所有修改完成后单击下方的 按钮，保存修改后的数据。
3. 在 Navicat for MySQL 中删除记录
图 2-28 “打开表”菜单命令
在打开的“学生表”结果窗体中用鼠标单击选中要删除的记录行，用鼠标右击该行记录，在弹出的菜单中选择“删除记录”命令菜单项，如图2-30 所示，则会出现如图 2-31 所示的删除记录确认对话框，单击“删除一条记录”则删除所选的记录，单击“取消”则删除失败。
图 2-30 在编辑状态下删除记录 图 2-31 “确认删除”对话框删除表中已有记录行时要注意以下几点：
(1) 记录删除之后是不能恢复的，即删除是永久的，删除之前一定要确认无误；(2) 一次可以删除多条记录，按住“Shift”或“Ctrl”键，可以选择多条记录删除；(3) 在选好记录后，也可以按“Delete”键删除记录；
(4) 如果删除的记录是其他表的外键指向，删除操作可能会影响另外一个表即外键表。例如在学生表中删除一个学生记录时，因为学生表的学号是主键，而成绩表中的学号是外键，则这个学生的所有成绩记录有可能也被删除了，这要看外键定义时的具体情况而定。4. 在 Navicat for MySQL 中查看记录
启动“Navicat for MySQL”，双击连接名“localhost_3306”建立连接，在“连接”面板中双击“学生成绩管理”数据库，展开“表”节点，鼠标右键单击其下的“学生表”，在打开的菜单中选择“打开表”菜单项，即可查看“学生表”中的所有记录。
5. 测试约束的作用
前面我们介绍了建立表命令的简单格式及在表后加主键、外键、非空、唯一、默认、自动增加列约束的方法，那么我们如何检查这些约束是否起作用呢？唯一的办法是实践！请在表中添加符合表中所有约束的数据记录，系统让接收这样的记录放入表中，而输入不符合表中某一个或多个约束的记录行时，系统拒绝让数据表接收这样的记录行，这样数据表中的约束起作用了。
(1) 在学生表中主键设置为学号，其他字段设置为允许空，输入两个同样的学号，并点击保存，系统将会提示错误，请仔细阅读错误提示内容。
(2) 将学生表中将姓名列添加唯一约束，输入两个同样的学生姓名，并点击保存，系统将会提示错误，请仔细阅读错误提示内容。
(3) 将学生表中将性别列添加默认值约束，默认值设置为“女”，输入一个新的学生学号和姓名，并点击保存，系统将会为性别列填入默认值“女”。
(4) 将“所在系”列设置为非空，输入一个新的学生学号和姓名，并点击保存，系统将会提示错误，请仔细阅读错误提示内容。
(5) 在成绩表中填入一个学生表中已经存在的学号，和一个课程表中不存在的课程号，，并点击保存，系统将会提示错误，请仔细阅读错误提示内容。
2.5 任务 5：图形界面下管理用户与权限2.5.1 相关知识
数据库安全控制的方法主要有用户标识与鉴别、存取权限控制、视图机制、审计、数据加密等。
1. 用户标识与鉴别
系统用户标识与鉴别是系统提供的最外层安全保护措施。当用户要求进入系统时，都要输入用户标识，系统进行核对后，对于合法的用户才提供机器使用权。获得了机器使用权的用户不一定具有数据库的使用权，数据库管理系统还要进一步进行用户标识和鉴定，以拒绝没有数据库使用权的用户（非法用户）进行数据库的存取操作。用户标识和鉴定的方法有很多种，而且在一个系统中往往是多种方法并举，以获得更强的安全性。通常使用用户名和口令标识鉴定用户，用户标识与鉴别可以重复多次。
2. 存取权限控制
数据库安全最重要的一点就是确保只授权给合法的用户访问数据库，同时令所有未被授权的人员无法接近数据库，这主要通过数据库系统的存取权限控制机制实现。存取权限控制机制主要包括定义用户权限和合法权限检查两部分：
·定义用户权限，并将用户权限登记到数据字典中。用户权限是由两个要素组成的：数据对象和操作类型，定义一个用户的存取权限就是要定义这个用户可以在哪些数据对象上进行哪些类型的操作。
·合法权限检查，每当用户发出存取数据库的操作请求之后，DBMS 首先查找数据字典，根据安全规则进行合法权限检查，若用户的操作请求超出了定义的权限，系统将拒绝执行此操作。
在数据库系统中定义存取权限称为授权。用户权限定义中数据对象范围越小，授权子系统就越灵活。例如授权定义可精细到字段级，而有的系统只能对关系授权，授权粒度越细，授权子系统就越灵活，但系统定义与检查权限的开销也会相应地增大。在某些系统中，还会提供基于角色的权限控制机制。角色（Role），可以认为是一些权限的集合，一直是存在各个数据库中，比如 Oracle、SQL Server、OceanBase 等。MySQL 自从 8.0 release 才引入角色这个概念。为用户赋予统一的角色，即把一个带有某些权限集合的角色分配给一个用户，那该用户就拥有了该角色所包含的所有权限，权限的修改直接通过角色来进行，无需为每个用户单独授权，大大的方便了权限管理。在MySQL 中用户也可以作为角色给其他用户赋予权限。
2. 数据库其他安全保障机制
（1）视图机制
进行存取权限控制时可以为不同的用户定义不同的视图，把数据对象限制在一定的范围内。也就是说，通过视图机制把要保密的数据对无权存取的用户隐藏起来，从而自动地对数据提供一定程度的安全保护（具体内容详见项目 6 任务 4）。视图机制使系统具有3 个优点：数据安全性、逻辑数据独立性和操作简便性。
（2）数据库审计
所谓审计是把用户对数据库的所有操作自动记录下来放入审计日志中。如果怀疑数据库被篡改了，如哪个用户执行了更新和什么时候执行的更新等，那么就开始执行DBMS 的审计软件。该软件将扫描审计追踪中某一时间段内的日志，以检查所有作用于数据库的存取动作和操作，当发现一个非法的或未授权的操作时，DBA（DataBase Administrator，数据库管理员）就可以确定执行这个操作的账号。
审计通常是很费时间和空间的，所以 DBMS 往往都将其作为可选特征，允许DBA根据应用对安全性的要求，灵活地打开或关闭审计功能，审计功能一般主要用于安全性要求较高的部门。
（3）数据加密
对于高度敏感性数据，如财务数据、军事数据、国家机密，除以上安全性措施外，还可以采用数据加密技术。数据加密是防止数据库中数据在存储和传输中失密的有效手段。加密的基本思想是根据一定的算法将原始数据（明文）变换为不可直接识别的格式（密文），从而使不知道解密算法的人无法获知数据的内容。
2.5.2 任务实施
要完成以下操作，当前操作的用户必须具有对应的权限。
1. 新建用户
图 2-32 查看用户
双击打开连接，然后点击“用户”图标，右边视窗将会显示该连接对应的服务器上的用户，如图2-32 所示。注意，该连接的账户应具有查看“用户”信息的权限。点击“新建用户”按钮，打开“用户”窗口。
图 2-33 添加用户
该窗口常规选项卡可以添加“用户名”、“主机”、“密码”等信息，如图 2-33 所示。点击“服务器权限”选项卡，打开权限列表窗口。
图 2-34 查看和修改用户权限
在该窗口中，可以通过“授予”列的复选框给该用户授予（即勾选）或回收（撤销勾选）权限，如图2-34
所示。此处授予的权限是针对服务器上所有数据库的权限。图 2-35 查看和修改用户权限
点击“权限”选项卡，可对该用户在指定数据库上进行授权和权限回收，如图 2-35 所示。点击“添加权限”按钮，打开“添加权限”窗口。图 2-36 添加权限窗口
左侧勾选指定的数据库，右侧打卡权限列表，勾选对应权限后的复选框可对该用户授予权限，如图2-36 所示。完成之后点击“确定”，窗口关闭。
回到如图 2-34 窗口，点击“保存”，完成对该用户的信息和权限的修改。读者可以自行测试使用新建用户连接服务器。此处不再赘述。
2. 查看和修改用户信息
图 2-37 查看和修改用户名和密码
在图 2-31 的窗口中，双击右边窗口中需要查看的账户，将显示该用户详细信息窗口，如图2-37 所示。该窗口显示了账户的详细信息，在常规选项卡可以修改“用户名”、“主机”、“密码”等信息。还可以对“服务器权限”、“权限”等选项卡进行操作。3. 删除用户
图 2-38 删除用户
在图 2-38 的navicat 主窗口中，点击右边窗口中需要删除的账户，点击“删除用户”按钮。该窗口显示了账户的详细信息，在常规选项卡可以修改“用户名”、“主机”、“密码”等信息。图 2-39 删除用户确认窗口
在弹出的窗口中，点击“删除”完成删除用户。





综合练习题

（一）、填空题

1．若一个联系涉及到两个实体则称为 二元 联系，若只涉及到一个实体则称为 自 联系。

2．在实体中能作为码的属性称为 主属性，否则称为 非主属性。

3．域是实体中相应属性的 取值范围，性别属性的域包含有 2 个值。

4．实体之间的联系类型有三种，分别为 一对一、一对多 和 多对多。

5．若实体A和B是多对多的联系，实体B和C是1对1的联系，则实体A和C是 多对多 的联系。

6．若实体A和B是1对多的联系，实体B和C是1对多的联系，则实体A和C是 1对多 的联系。

7．若实体A和B是1对多的联系，实体B和C是1对1的联系，则实体A和C是 1对多 的联系。

8．客户端登录MySQL服务的方法，尽管形式有别，但其实登录时都需要给出MySQL数据库服务器的名称（默认是localhost），登录用户名（默认是root）和 密码。

9．查看MySQL数据库的存储引擎，可以在控制台中执行 SHOW ENGINES 命令。

10．当前最新的MySQL版本，默认的存储引擎是 InnoDB。

11．在控制台中执行“USE 数据库名;”语句表示选择当前要操作的数据库，执行成功后可以向数据库中添加表和视图等内容。

12．一个表上只能创建 1 个主键约束，但可以创建 多个 唯一性约束。

13．在MySQL中，我们用 NULL 表示数值未知的空值。

14．数据完整性可分为四种类型，分别是：实体完整性、域完整性、引用完整性 和 用户定义的完整性。

15．在SQL中，创建数据库的命令是 CREATE DATABASE，修改数据库命令是 ALTER DATABASE，删除数据库命令是 DROP DATABASE。

16．在SQL中，创建数据表的命令是 CREATE TABLE，修改数据表的命令是 ALTER TABLE，删除数据表的命令是 DROP TABLE。

17．外键约束是表中数据之间的一种引用关系，即一个表中的 主键 与另一个表中的列（外键）之间的一对多关系的体现。外键的定义涉及到至少两个表，在表的定义中通过 FOREIGN KEY 关键字来实现。

18．使用CREATE TABLE命令创建表时可以使用 AUTO_INCREMENT 属性来定义标识列。标识列一般都用作为主键列，因为在同一个表的标识列中不会有两个相同的标识值。

19．在SQL语言中可以使用 INSERT INTO 语句往数据库的表中插入一行或多行数据记录。

20．在SQL语言中可以使用 DELETE 语句删除表中部分或全部数据记录。

21．在SQL语言中可以使用 UPDATE 语句更新表中数据记录值。

22．在MySQL 中，索引有 普通索引、唯一索引、主键索引、全文索引、空间索引等。

23．每个表上最多可以创建 1 个聚集索引，多个 个非聚集索引。

24．唯一索引也是 唯一性 约束，在建立了唯一索引的字段最多可以有 1 个 NULL 值。

25．在表中创建索引时，可以包含多个字段。这种将多个字段组合起来建立的索引，称之为 组合索引（复合索引）。

26．事务回滚是指当事务中的某一语句执行失败时，将对数据库的操作恢复到事务执行前或某个指定位置。事务回滚使用 ROLLBACK 命令。

27．默认情况下，在MySQL中直接写的SQL语句都是 自动 提交的。

28．读未提交 是事务中最低的级别，该级别下的事务可以读取到另一个事务中未提交的数据，也被称为 脏读。

29．存储过程 是一组预先写好的能实现某种功能的T-SQL程序代码，指定一个名称并经过编译后将其保存在MySQL服务器中。

30．在MySQL存储过程中参数分为三类，分别是 IN、OUT 和 INOUT。

31．条件处理的方式有 IF 和 CASE 语句来完成。

32．触发器是一种特殊的 存储过程，其特殊之处在于触发器不是调用执行，而是当有关事件发生时 自动 执行的。



(二)、单项选择题

1．数据库、数据库管理和数据库系统之间的关系正确的是__________。
A．数据库包括了数据库管理系统和数据库系统
B．数据库管理系统包括了数据库和数据库系统
C．数据库系统包括数据库和数据库管理系统
D．以上都不对
正确答案是：C

2．目前，商品化的数据库管理系统以__________型为主。
A．关系
B．层次
C．网状
D．对象
正确答案是：A

3．从给定关系中找出满足一定条件的元组的运算，称为________运算。
A．集合
B．投影
C．连接
D．选择
正确答案是：D

4．假设一位教师可以讲授多门课程，一门课程可由多位教师讲授，则教师与课程之间是___________。
A．一对一关系
B．一对多关系
C．多对多关系
D．其它关系
正确答案是：C

5．关于数据库的执行操作，_____________选项是错误的。
A．SHOW DATABASES语句用于查询当前数据库系统中已经存在的数据库
B．ALTER DATABASE语句用于修改指定数据库的名称
C．DROP DATABASE语句可以删除指定的数据库
D．CREATE DATABASE语句可以创建指定的数据库，并且在创建时可以设置字符集和校对规则
正确答案是：B

6．________数据类型的数据只能从自身列表中取值，并且只能取一个。
A．BOLB
B．TEXT
C．ENUM
D．BIT
正确答案是：C

7．如果某字段希望存放客户的家庭或办公电话，那么该字段应该采用_______数据类型。
A．char(10)
B．varchar(13)
C．text
D．int
正确答案是：B

8. MySQL 中数据表的物理存储包括两类文件_______________。
A．.myd文件、.ibd文件
B．.frm文件、.ibd文件
C．.trg文件、.ibd文件
D．.myi文件、.ibd文件
正确答案是：B

9． MySQL有___________个系统数据库。
A． 3
B． 4
C． 5
D． 6
正确答案是：B

10． 按照表的用途来分类，表可以分为________________两大类。
A． 数据表和索引表
B． 系统表和数据表
C． 用户表和非用户表
D． 系统表和用户表
正确答案是：D

11．数据库文件的扩展名为____________________。
A． .sql
B． .db
C． .mdf
D． .ldf
正确答案是：B

12．设计表窗口的栏位选项卡中的“不是null”单元格用于设置该列是否可输入空值，实际上就是创建该列的_________约束。
A．主键
B．外键
C．非空
D．唯一
正确答案是：C

13．可以说，表中主键约束是非空约束和_______的组合。
A．检查约束
B．唯一约束
C．外键约束
D．默认值约束
正确答案是：B

14．在SQL中字符串连接函数是________。
A．CONCAT
B．TRIM
C．SUBSTRING
D．LOCATE
正确答案是：A

15．CURDATE函数的作用是________。
A．返回日期中的天数
B．返回当前日期和时间
C．返回日期中的年份值
D．返回当前日期中的月份值
正确答案是：B





第1次形成性考核

一、填空题

1．一个数据库系统应该具有的功能有：使用 定义 语言建立数据库；使用 查询 语言对数据库中的数据进行查询；使用 更新 语言对数据库中的数据进行更新；支持存储大量的 数据，保证其正确安全使用；支持 多用户 并发访问且不相互影响和损坏数据。

2．数据模型是数据特征的抽象，是数据库管理的形式框架，是数据库系统中用以提供信息表示和操作手段的形式构架。数据模型所描述的内容包括三个部分：结构、操作 和 约束。

3．层次模型、网状模型 和 关系模型 是三种重要的数据模型。但现在 关系模型 占据了数据库的主导地位。

4．关系数据模型是以 集合 理论为基础的，用 关系 来表示实体以及实体之间联系。

5．在实体中能作为码的属性称为 主属性，否则称为 非主属性。

6．域是实体中相应属性的 取值范围，性别属性的域包含有 2 个值。

7. 按照用户群进行分类，MySQL分为 社区 版和 企业 版两大类。



二、单项选择题

1．数据库、数据库管理和数据库系统之间的关系正确的是__________。
A．数据库包括了数据库管理系统和数据库系统
B．数据库管理系统包括了数据库和数据库系统
C．数据库系统包括数据库和数据库管理系统
D．以上都不对
正确答案是：C

2．目前，商品化的数据库管理系统以__________型为主。
A．关系
B．层次
C．网状
D．对象
正确答案是：A

3．如果关系R属于1NF，并且R的每一个非主属性（字段）都完全依赖于主键，则R属于_______________。
A．1NF
B．2NF
C．3NF
D．4NF
正确答案是：B

4．关于常见的存储引擎，下面描述错误的是_____________。
A．InnoDB存储引擎虽然不支持事件处理应用程序，但是支持外键、同时还支持崩溃修复能力和并发控制
B．MEMORY存储引擎的所有数据都存储在内存中，数据的处理速度快但安全性不高
C．MyISAM存储引擎提供了高速的存储与检索和全文探索能力，它并不支持事务处理应用程序
D．除了InnoDB、MOMORY和MyISAM存储引擎外，MRG_MYISAM、BLACKHOLE和CSV也是MySQL数据库的存储引擎
正确答案是：D


5．查看数据库系统中已经存在的数据库时，可以执行_____________命令。
A．SHOW CREATE DATABASE;
B．SHOW CREATE DATABASES;
C．SHOW DATABASES;
D．SHOW DATABASE;
正确答案是：C

6．关于数据库的执行操作，_____________选项是错误的。
A．SHOW DATABASES语句用于查询当前数据库系统中已经存在的数据库
B．ALTER DATABASE语句用于修改指定数据库的名称
C．DROP DATABASE语句可以删除指定的数据库
D．CREATE DATABASE语句可以创建指定的数据库，并且在创建时可以设置字符集和校对规则
正确答案是：B

7. MySQL 中数据表的物理存储包括两类文件_______________。
A．.myd文件、.ibd文件
B．.frm文件、.ibd文件
C．.trg文件、.ibd文件
D．.myi文件、.ibd文件
正确答案是：B

8． MySQL有___________个系统数据库。
A． 3
B． 4
C． 5
D． 6
正确答案是：B

9． 按照表的用途来分类，表可以分为________________两大类。
A． 数据表和索引表
B． 系统表和数据表
C． 用户表和非用户表
D． 系统表和用户表
正确答案是：D

10．数据库文件的扩展名为____________________。
A．.sql
B． .db
C． .mdf
D． .ldf
正确答案是：B





第2次形成性考核

一、填空题

1．SQL语言的四类核心语句。这四类语句分别是：数据查询语句、数据定义 语句、数据控制 语句和 数据操作 语句。

2．在MySQL数据库中，要访问任何一个对象都要通过其名称来完成，在SQL语言中，对数据库、表、变量、存储过程、函数等的定义和引用都需要通过 标识符 来完成。

在SQL中，创建数据库的命令是 CREATE DATABASE，修改数据库命令是 ALTER DATABASE，删除数据库命令是 DROP DATABASE。
4．在SQL语言中可以使用 INSERT 语句往数据库的表中插入一行或多行数据记录。

5．在SQL语言中可以使用 DELETE 语句删除表中部分或全部记录。

6．在SQL语言中可以使用 UPDATE 语句更新表中数据记录值。



二、单项选择题

1．使用SQL语句创建表时，语句是________。
A．ADD TABLE
B．ALTER TABLE
C．DROP TABLE
D．CREATE TABLE
正确答案是：D

2．可以说，表中主键约束是非空约束和_______的组合。
A．检查约束
B．唯一约束
C．外键约束
D．默认值约束
正确答案是：B

3．在SQL中字符串连接函数是________。
A．CONCAT
B．TRIM
C．SUBSTRING
D．LOCATE
正确答案是：A

4．CURDATE函数的作用是________。
A．返回日期中的天数
B．返回当前日期和时间
C．返回日期中的年份值
D．返回当前日期中的月份值
正确答案是：B

5．使用SQL命令建立表时，每列的定义中必不可少的内容是________。
A．列名和长度
B．类型和长度
C．列名和约束
D．列名和类型
正确答案是：D



第4次形成性考核

一、填空题

1．索引（index）是依赖表建立的，它的作用是用来提高数据的检索速度。

3．每个表上最多可以创建1个聚集索引，无限多个非聚集索引。

4．唯一索引也是唯一约束，在建立了唯一索引的字段最多可以有1个NULL值。

6．视图是由SELECT语句构成的，是基于表的虚拟表。

7．数据库中只存储视图的定义，即视图中的数据是由哪些表中的哪些数据列组成的，视图不生成所选数据库行和列的永久拷贝。

1．事务作为一个逻辑单元，必须具备4个属性，分别是：原子性、一致性、隔离性和持久性。

3．事务回滚是指当事务中的某一语句执行失败时，将对数据库的操作恢复到事务执行前或某个指定位置。事务回滚使用ROLLBACK命令。

4．默认情况下，在MySQL中直接写的SQL语句都是自动提交的。

1．存储过程是一组预先写好的能实现某种功能的T-SQL程序代码，指定一个名称并经过编译后将其保存在MySQL服务器中。

4．触发器是一种特殊的存储过程，其特殊之处在于触发器不是调用执行，而是当有关事件发生时自动执行的。

10．和 MS SQL Server 中的 INSERTED 和 DELETED 类似，MySQL 中定义了NEW和OLD对象，用来表示触发器的所在表中，触发了触发器的那一行数据。





形成性考核

（）由数据结构. 关系操作集合和完整性约束三部分组成：
选择一项：
a. 关系数据库
b. 关系模式 
c. 关系
d. 关系模型
正确答案是：关系模式

DECIMAL是（ ）数据类型
选择一项：
a. 整数值
b. 双精度浮点值
c. 单精度浮点值
d. 可变精度浮点值 
正确答案是：可变精度浮点值

GROUP_CONCAT（）函数作用是（ ） .
选择一项：
a. 统计每组的平均值
b. 将每组的结果字符串连接起来 
c. 将每组的结果累加
d. 统计每组的记录数
正确答案是：将每组的结果字符串连接起来

int类型数据范围是 （ ）
选择一项：
a. -9223372036854775808---922337203685477580718446744073709551615
b. -128----127 0---255
c. -8388608---8388607 0---16777215
d. -2147483648---2147483647 0---4294967295 
正确答案是：-2147483648---2147483647 0---4294967295

MySQL中，预设的、拥有最高权限超级用户的用户名为彩蛋
选择一项：
a. test
b. Administrator
c. root 
d. DA
正确答案是：root

MySQL中唯一索引的关键字是（）
选择一项：
a. fulltextindex
b. uniqueindex 
c. onlyindex
d. index
正确答案是：uniqueindex

MySQL中存储用户全局权限的表是（）
选择一项：
a. table_priv
b. user 
c. columns_priv
d. procs_priv
正确答案是：user

MySQL数据库是（ ）型数据库
选择一项：
a. 网状
b. 线
c. 层次
d. 关系 
正确答案是：关系

MySQL数据库管理系统，可以运行于（ ）操作系统
选择一项：
a. Windows
b. 都可以 
c. UNIX
d. Linux
正确答案是：都可以

MySQL有（ ）个系统数据库
选择一项：
a. 6
b. 5
c. 4 
d. 3
正确答案是：4

在SQL语言中，MAX作用是（ ）
选择一项：
a. 最小值
b. 平均值
c. 和
d. 最大值 
正确答案是：最大值

在SQL语言中，SUM作用是（ ）
选择一项：
a. 最大值
b. 个数
c. 求和 
d. 平均值
正确答案是：求和

在SQL语言中，视图是数据库体系结构中的（ ）。
选择一项：
a. 物理模式
b. 外模式 
c. 内模式
d. 模式
正确答案是：外模式

在student表录入数据时，常常需要一遍又一遍的输入"男”到学生"性别”列，以下（ ）种方法可以解决这个问题？
选择一项：
a. 创建一个UNIQUE约束（或唯一值 ）
b. 创建一个PRIMARY KEY约束（或主键）
c. 创建一个CHECK约束
d. 创建一个DEFSULT约束（ 或默认值 ） 
正确答案是：创建一个DEFSULT约束（ 或默认值 ）

在一个关系中，如果有这样一个属性组存在，它的值能唯一的标识此关系中的一个元组，该属性组称为（ ）。
选择一项：
a. 数据项
b. 主键 
c. 主属性
d. 主属性值
正确答案是：主键

在关系代数中，对一个关系做投影操作后，新关系的元组个数（ ）原来关系的元组个数：
选择一项：
a. 等于
b. 小于
c. 小于或等于 
d. 大于
正确答案是：小于或等于

在关系模式中，实现"关系中不允许出现相同的元组”的约束是（ ）约束。
选择一项：
a. 外键
b. 主键 
c. 键
d. 候选键
正确答案是：主键

在利用计算机进行数据处理的四个发展阶段中，第一个发展阶段是（）。
选择一项：
a. 分布式数据库系统
b. 数据库系统
c. 人工管理 
d. 文件系统
正确答案是：人工管理

在利用计算机进行数据处理的四个发展阶段中，第三个发展阶段是（ ）。
选择一项：
a. 分布式数据库系统
b. 数据库系统 
c. 人工管理
d. 文件系统
正确答案是：数据库系统

在对SQL语言的描述中，正确的是（ ）。
选择一项：
a. SQL是一种过程化语言
b. SQL不能嵌入到高级语言程序中
c. SQL 是一种 DBMS
d. SQL集合操作方式 
正确答案是：SQL集合操作方式

关系代数中的连接操作是由（）操作组合而成 。
选择一项：
a. 投影. 选择. 笛卡尔积
b. 投影和笛卡尔积
c. 选择和笛卡尔积 
d. 选择和投影
正确答案是：选择和笛卡尔积

关系代数运算是以（ ）为基础的运算 。
选择一项：
a. 代数运算
b. 关系运算
c. 集合运算 
d. 谓词演算
正确答案是：集合运算

关系数据库中，主键是（ ）
选择一项：
a. 创建唯一的索引，允许空值
b. 只允许以表中第一字段建立
c. 为标识表中唯一的实体 
d. 允许有多个主键的
正确答案是：为标识表中唯一的实体

关系数据库中，实现实体之间的联系是通过表与表之间的（ ）
选择一项：
a. 公共属性 
b. 公共索引
c. 公共元组
d. 公共存储
正确答案是：公共属性

关系数据库管理系统应能实现的专门关系运算包括（）。
选择一项：
a. 关联. 更新. 排序
b. 排序. 索引. 统计
c. 显示. 打印. 制表
d. 选择. 投影. 连接 
正确答案是：选择. 投影. 连接

关系数据模型的三个组成部分中，不包括（ ）
选择一项：
a. 完整性约束
b. 数据维护 
c. 数据结构
d. 数据操作
正确答案是：数据维护

关系模型中实现实体间 N：M 联系是通过增加一个（） 。
选择一项：
a. 关系或一个属性实现
b. 关系实现 
c. 关系和一个属性实现
d. 属性实现
正确答案是：关系实现

关系模型中有三类完整性约束：实体完整性. 参照完整性和用户自定义完整性。定义外部关键字实现的是哪一类完整性（ ）
选择一项：
a. 用户自定义完整性
b. 参照完整性 
c. 实体完整性
d. 实体完整性. 参照完整性
正确答案是：参照完整性

创建用户的命令是（）
选择一项：
a. create user 
b. join user
c. mysql user
d. create root
正确答案是：create user

删除用户的命令是（）
选择一项：
a. truncate user
b. delete user
c. drop user 
d. drop root
正确答案是：drop user

从关系中找出满足一定条件的元组运算，称为（ ）运算
选择一项：
a. 投影
b. 选择 
c. 连接
d. 集合
正确答案是：选择

以下哪种情况应尽量创建索引（ ）。
选择一项：
a. 在Where子句中出现频率较高的列 
b. 具有很多NULL值的列
c. 需要更新频繁的基本表
d. 记录较少的基本表
正确答案是：在Where子句中出现频率较高的列

以下哪种操作能够实现参照完整性（ ）
选择一项：
a. 设置主键
b. 设置唯一键
c. 设置外键 
d. 减少数据冗余
正确答案是：设置外键

以下是数据库管理系统的是（ ）。
选择一项：
a. SQLite 
b. Android
c. Navicat
d. Office
正确答案是：SQLite

以下聚合函数求和是（ ）
选择一项：
a. MAX
b. COUNT
c. SUM 
d. AVG
正确答案是：SUM

以下聚合函数求平均数的是（ ）
选择一项：
a. COUNT
b. MAX
c. AVG 
d. SUM
正确答案是：AVG

以下聚合函数求数据总和的是（ ）
选择一项：
a. MAX
b. SUM 
c. AVG
d. COUNT
正确答案是：SUM

以下聚合函数求最小值的是()
选择一项：
a. MIN 
b. SUM
c. MAX
d. COUNT
正确答案是：MIN

以下软件（ ）是数据库管理系统
选择一项：
a. WeChat
b. MySQL 
c. QQ
d. Android
正确答案是：MySQL

修改自己的mysql服务器密码的命令是（）
选择一项：
a. mysql
b. changepassword
c. grant
d. setpassword 
正确答案是：setpassword





01-1 、目前，商务化的数据库管理系统是以（ ）型为主。
选择一项：
a.层次
b.关系
c.网状
d.对象
正确答案是：关系

01-3 、MySQL数据库是（ ）型数据库。
选择一项：
a.层次
b.网状
c.关系
d.线
正确答案是：关系

01-5 、___________也是数据库系统的核心和基础，各种机器上实现的DBMS软件都是基于某种___________的。 依此填入的内容是：
选择一项：
a.数据；模型
b.数据模型；数据
c.数据；数据模型
d.数据模型；数据模型；
正确答案是：数据模型；数据模型；

01-10 、MySQL有（ ）个系统数据库
选择一项：
a.3
b.5
c.4
d.6
正确答案是：4

01-11 、以下哪个软件不是数据库管理系统软件（ ）
选择一项：
a.SQL Server
b.MySQL
c.Office
d.Oracle
正确答案是：Office

01-15 、数据库、数据库管理和数据库体统之间的关系正确的是：
选择一项：
a.数据库管理系统包括数据库和数据库系统
b.以上都不对
c.数据库系统包括数据库和数据库管理系统
d.数据库包括数据库系统和数据库管理系统
正确答案是：数据库系统包括数据库和数据库管理系统

01-16 、
现在（ ）占据了数据库的主导地位。
选择一项：
a.树形模型
b.网状模型
c.关系模型
d.层次模型
正确答案是：关系模型

01-39 、一个数据库系统应该具有的功能有：
选择一项或多项：
a.使用数据操作或查询语言对数据库中的数据进行查询和更新。
b.支持多用户并发访问且不相互影响和损坏数据。
c.支持存储大量的数据，保证对数据的安全使用。
d.使用数据定义语言建立数据库。
正确答案是：ABCD

01-41 、数据模型是对客观事物抽象化的表现形式，它具有三大特点：
选择一项或多项：
a.要便于理解，使用者与设计者要取得一致的看法；
b.应该便于使用计算机来实现和处理。
c.它必须真实地反映现实世界中的具体应用，否则就失去了意义；
d.它必须有足够的安全性。
正确答案是：
	它必须真实地反映现实世界中的具体应用，否则就失去了意义；
	要便于理解，使用者与设计者要取得一致的看法；
	应该便于使用计算机来实现和处理。

01-42 、关系数据模型由（ ）组成。
选择一项或多项：
a.关系存储集合
b.关系数据结构
c.关系操作集合
d.关系完整性约束
正确答案是：
	关系数据结构
	关系操作集合
	关系完整性约束

01-43 、专门的关系运算主要有：
选择一项或多项：
a.投影
b.连接
c.选择
d.综合运算
正确答案是：
	选择
	投影
	连接

01-45 、以下关于“数据库”的概念，正确的是~
选择一项或多项：
a.数据库中的这些数据是结构化的，无有害的或不必要的冗余，并为多种应用服务。数据的存储独立于使用它的程序。
b.数据库是存储在一起的相关数据的集合。
c.当某个系统中存在结构上完全分开的若干个数据库时，则该系统包含一个"数据库集合”。
d.对数据库插入新数据，修改和检索原有数据均能按一种公用的和可控制的方式进行。
正确答案是：ABCD

01-20 、数据模型是对现实世界数据特征的抽象，从现实世界的事物客观特性到计算机里的具体表示将经历现实世界、信息世界和机器世界3个数据领域。
选择一项：
对
错
正确答案是“对”。

01-21 、分布式数据库系统是一个逻辑上分散、地域上分布的数据集合，是计算机网络环境中各个局部数据库的逻辑集合，同时受分布式数据库管理系统的控制和管理。
选择一项：
对
错
正确答案是“错”。

01-24 、P1∪P2表示关系P1与P2的交。
选择一项：
对
错
正确答案是“错”。

01-28 、我们将关系的名称和关系的属性名称的集合称为该关系的模式。如：课程（课程号，课程名称，学分，课程性质）。
选择一项：
对
错
正确答案是“对”。

01-31 、每个属性的取值范围所对应的值的集合称为该属性的域。
选择一项：
对
错
正确答案是“对”。

01-35 、在关系代数中，π表示选择，下标表示选择的属性。
选择一项：
对
错
正确答案是“错”。

01-36 、关系是笛卡尔乘积的有一定意义的、有限的子集。
选择一项：
对
错
正确答案是“对”。

01-37 、一个关系可以看作是一个二维表。
选择一项：
对
错
正确答案是“对”。



02-1 、在画E-R图时，学号和身份证号的联系是：
选择一项：
a.1：N
b.1：1
c.以上都不对
d.N：N
正确答案是：1：1

02-2 、有如下表： 选课表（学生，课程，老师，教材，教室） 那么该表满足
选择一项：
a.3NF
b.2NF
c.1NF
d.4NF
正确答案是：1NF

02-4 、假设选课系统将学生信息和课程信息保存在一个关系表：学生选课表（学号，姓名，性别，所在系，系主任，课程名，课程号，成绩），关键字为（学号，课程号）。
成绩（1）依赖（学号，课程）。
姓名（2）依赖（学号，课程） 。
系主任（3）依赖学号。
A.部分
B.完全
C.传递
依次填入的选项是：
选择一项：
a.B,C,A
b.A,B,C
c.A,C,B
d.B,A,C
正确答案是：B,A,C

02-5 、假设一个教师可以讲授多门课程，一门课程可以由多位老师讲授，则教师与课程之间是（ ）关系。
选择一项：
a.多对一
b.一对多
c.多对多
d.一对一
正确答案是：多对多

02-7 、有3个实体学生、班级、课程，1个一对多的联系，1个多对多的联系，转化成数据库的逻辑结构时变成（ ）。~
选择一项：
a.6个关系
b.5个关系
c.3个关系
d.4个关系
正确答案是：4个关系

02-8 、在下面教师表和系部表中，教师号和系部号分别是两个表的主键 教师表（教师号，姓名，性别，系部号） 系部表（系部号，系部名，部门人数，部门描述） 在这两个表中，只有一个是外键，它是（ ） ~
选择一项：
a.教师表中的"系部号”
b.系部表中的"系部号”
c.教师表中的"教师号”
d.系部表中的"系部名”
正确答案是：教师表中的"系部号”

02-9 、有如下表： 选课表（学生，课程，老师，教材，教室） 那么该表满足
选择一项：
a.1NF
b.2NF
c.3NF
d.4NF
正确答案是：1NF

02-10 、________是用二维表的形式表示实体和实体间联系的数据模型。 ~
选择一项：
a.数据模型；
b.关系模型；
c.链表模型；
d.层次模型；
正确答案是：关系模型；

02-50 、有如下表： 选课表（学生，课程，老师，职称，教室） 那么该表满足：
选择一项或多项：
a.4NF
b.3NF
c.1NF
d.2NF
正确答案是：1NF, 2NF

02-51 、（ ）是E-R图的最基本的概念。
选择一项或多项：
a.关系
b.实体
c.联系
d.属性
正确答案是：实体, 属性, 联系

02-42 、同一类型的实体称为实体集，如（    ）。
选择一项或多项：
a.所有新闻稿件组成新闻稿实体集。
b.所有学生组成学生实体集。
c.所有商品组成商品实体集。
d.课程组成课程实体集。
正确答案是：ABCD

02-43 、以下关于E-R图的元素的说明正确的是：
选择一项或多项：
a.连线代表联系
b.矩形代表实体。
c.椭圆代表属性。
d.E-R用于概念设计阶段。
正确答案是：ABCD

02-44 、在画E-R图时，下面的联系是1：N的
选择一项或多项：
a.学号和身份证号
b.教工号和教师的手机号
c.教工号和课程号
d.班主任教工号和班级号
正确答案是：
	教工号和教师的手机号
	班主任教工号和班级号

02-46 、需求分析的基本步骤。主要包括以下方面：
选择一项或多项：
a.设计人员了解熟悉用户的业务活动，进行系统需求的收集。
b.确定系统边界，定义数据字典和数据流图。
c.设计人员向用户了解企业或单位的组织机构的总体情况。
d.设计人员确定用户需求的全部内容，撰写需求说明书。
正确答案是：ABCD

02-47 、有如下表： 选课表（学生，课程，老师，教材，教室） 那么下面的函数关系成立的是：
选择一项或多项：
a.（学生，课程）——>老师；
b.课程——>上课时间；
c.（学生，课程）——>教室；
d.课程——>教材；
正确答案是：
	（学生，课程）——>教室；
	（学生，课程）——>老师；
	课程——>教材；

02-11 、关系的主键又称为主属性。
选择一项：
对
错
正确答案是“错”。

02-13 、如果关系模式R属于第一范式，且每个非主属性都不传递依赖于关键字，则R属于第三范式。
选择一项：
对
错
正确答案是“对”。

02-15 、在需求分析阶段应该把业务的流程调查清楚，并画出实体-联系图。
选择一项：
对
错
正确答案是“错”。

02-28 、关系S1（学号，系名，系主任），学号 → 系名，系名 → 系主任，并且系名 ！→ 学号，系主任 ！→ 系名，所以学号 → 系主任为传递函数依赖。
选择一项：
对
错
正确答案是“对”。

02-39 、在1NF的基础上，消除了非主属性对关键字的完全函数依赖关系，即可得到2NF。
选择一项：
对
错
正确答案是“错”。



03-1 、当前最新版本的MySQL版本默认的存储引擎是：~
选择一项：
a.InnoDB
b.MRG_MyISAM
c.MyISAM
d.CSV
正确答案是：InnoDB

03-2 、当出现“1046 - No database selected”错误提示时，我们应该~
选择一项：
a.输入“USE 数据库名”指定当前要操作的数据库。
b.重启数据库。
c.输入“SHOW TABLES;”命令。
d.检查输入命令的语法错误。
正确答案是：输入“USE 数据库名”指定当前要操作的数据库。

03-3 、关于命令行的"Command Line Client”和"Command Line Client-Unicode”方式，说法正确的是。~
选择一项：
a."Command Line Client-Unicode”只适合远程客户端登录。
b."Command Line Client-Unicode”更适合中文的输入和输出。
c."Command Line Client-Unicode”更适合英文的输入和输出。
d.以上说法都不对。
正确答案是："Command Line Client-Unicode”更适合中文的输入和输出。

03-4 、MySQL数据库安装时默认的端口号是：
选择一项：
a.8080
b.80
c.3307
d.3306
正确答案是：3306

03-5 、安装MySQL数据库系统时，默认的系统管理员用户名是：
选择一项：
a.自己任意指定
b.administrator
c.admin
d.root
正确答案是：root

03-13 、下面属于MySQL数据库图形界面管理工具的是:
选择一项或多项：
a.MySQL Workbench
b.Command Line Client-Unicode
c.phpMyAdmin
d.Navicat for MySQL
正确答案是：
	MySQL Workbench
	Navicat for MySQL
	phpMyAdmin

03-14 、关于修改已经创建的数据库，说法正确的是：~
选择一项或多项：
a.可以修改数据库的字符集。如：ALTER DATABASE students_courses CHARACTER SET gd2312;
b.可以用Navicate修改已创建的数据库的库名。
c.可以修改数据库的名字。
d.已经创建的数据库不能修改库名，只能将其删除。
正确答案是：
	可以修改数据库的字符集。如：ALTER DATABASE students_courses CHARACTER SET gd2312;
	已经创建的数据库不能修改库名，只能将其删除。

03-15 、关于MySQL数据库存储引擎的特点说法正确的是？~
选择一项或多项：
a.InnoDB是当前版本的默认存储引擎。
b.MyISAM引擎不支持事务，提供高速存储和检索，以及全文搜索能力。
c.nnoDB相对于MyISAM来说，因为支持事务管理功能，所以对数据安全性、完整性的保障更好。
d.InnoDB相对于MyISAM来说，缺点是读写效率稍差，占用的数据空间相对较大。
正确答案是：ABCD

03-6 、进入服务器账号和角色设置（Accounts and Roles）窗口，首先要设置系统管理员用户root的密码，分别在MySQL root Password和Repeat Password两个文本框中输入相同的密码，进入服务器的用户名是root，密码就是此处设置的这个。
选择一项：
对
错
正确答案是“对”。

03-7 、可以在"管理工具”中的"服务”，在服务窗口找到MYSQL服务，并右键点击后选择启动或暂停。
选择一项：
对
错
正确答案是“对”。

03-8 、MyISAM是当前MySQL数据库版本的默认存储引擎，其最大的特色是支持ACID兼容的事务功能。
选择一项：
对
错
正确答案是“错”。

03-9 、系统默认的字符集是utf8，但在创建数据库时也可以指定字符集，使用关键字"DATABASE SET”后加字符集的名字。
选择一项：
对
错
正确答案是“错”。

03-10 、无论用哪种方式登录数据库，都必须给出: 服务器名称, 用户名, 密码。
选择一项：
对
错
正确答案是“对”。

03-11 、MySQL分为社区版（Community Server）和企业版（Enterprise）两大类。企业版是自由下载并且完全免费使用的，但是官方不提供任何技术支持，适用于大多数普通用户使用。
选择一项：
对
错
正确答案是“错”。

03-12 、连接（Connectivity）选项中，"TCP/IP”复选项选中表示启用TCP/IP网络协议，配置连接MySQL服务器的默认端口号为"3306”。
选择一项：
对
错
正确答案是“对”。



04-1 、按照表的用途来分类，表可以分为________________两大类。
选择一项：
a.用户表和非用户表
b.系统表和数据表
c.系统表和用户表
d.数据表和索引表
正确答案是：系统表和用户表

04-2 、________数据类型的数据只能从自身列表中取值，并且只能取一个。
选择一项：
a.BOLB
b.TEXT
c.ENUM
d.BIT
正确答案是：ENUM

04-3 、设计表窗口的栏位选项卡中的"不是null”复选框选中表示该字段不可输入空值，实际上就是创建该列的_________约束。
选择一项：
a.非空
b.唯一
c.外键
d.主键
正确答案是：非空

04-4 、如果要求表中的一个或多个字段的组合具有不重复的值，而且不允许为空，就应当将这个字段或字段的组合设置________。
选择一项：
a.检查约束
b.外键约束
c.唯一性约束
d.主键约束
正确答案是：唯一性约束

04-5 、在Navicat for MySQL工具中设置表的主键时，如果表中的多个字段组合才能成为主键，可以用________方法实现这个操作。
选择一项：
a.将所有主键字段添加主键标识
b.按住Tab键
c.按住Shift键
d.按住Ctrl键
正确答案是：将所有主键字段添加主键标识

04-6 、一个表上可以创建________个主键约束，________个唯一性约束。
选择一项：
a.1，多
b.多，多
c.1，1
d.多，1
正确答案是：1，多

04-7 、你向一个VARCHAR（40）型字段中输入数据"Hello world！”。当你以后从这个字段中取出此数据时，你取出的数据其长度为 个字符。
选择一项：
a.40
b.12
c.41
d.10
正确答案是：12

04-8 、假如你把"Hello world！”字符串输入CHAR（40）型字段中，那么当你取出数据时，所取出的数据长度将是（）个字符。
选择一项：
a.10
b.41
c.12
d.40
正确答案是：40

04-9 、ENUM('值1','值2','值3',...,'值n') 在上述格式中，('值1','值2','值3',...,'值n')称为枚举列表，ENUM类型的数据只能从枚举列表中取，并且只能取___________个。
选择一项：
a.3
b.0
c.2
d.1
正确答案是：1

04-10 、关于外关键字（外键或外码）说明正确的是：
选择一项：
a.是这个关系的属性，而且必须是这个关系的主键。
b.以上都不对。
c.外键仅仅是另一个关系中的关键字，可以不是这个关系的属性。
d.在包含多个关系的数据库中，如果这个关系中的某个属性不是关键字，而这个属性在另一个关系中却是关键字，则该属性为这个关系的外关键字。
正确答案是：在包含多个关系的数据库中，如果这个关系中的某个属性不是关键字，而这个属性在另一个关系中却是关键字，则该属性为这个关系的外关键字。

04-11 、设计学生数据表，存储姓名字段，要用（ ）型字段
选择一项：
a.字符
b.日期
c.数字
d.备注
正确答案是：字符

04-12 、设计学生数据表，存储 年龄 字段，要用（ ）型字段
选择一项：
a.字符
b.逻辑
c.日期
d.整型
正确答案是：整型

04-13 、 一般情况下，以下哪个字段可以作为主关键字（ ）
选择一项：
a.年龄
b.姓名
c.性别
d.学号
正确答案是：学号

04-14 、如果字段内容为年龄，可将此字段定义为（ ）类型
选择一项：
a.浮点
b.字符
c.整型
d.日期
正确答案是：整型

04-20 、数据完整性可分为四种类型，分别是：_______________。
选择一项或多项：
a.实体完整性
b.参照完整性
c.域完整性
d.用户定义的完整性
正确答案是：ABCD

04-15 、安装好MySQL后，会自动创建用户和用户数据库。
选择一项：
对
错
正确答案是“错”。

04-16 、日期和时间数据类型用于存储日期和时间，又细分为YEAR、DAT~ TIM~ DATETIME和TIMESTAMP五种。
选择一项：
对
错
正确答案是“对”。

04-17 、NULL即空值表示"空白”或者"0”，两个NULL值可能相等也可能不等。
选择一项：
对
错
正确答案是“错”。

04-18 、系统表是维护MySQL服务器和数据库正常工作的数据表，每个数据库下都会建立一些系统表，用户一般不要对系统表进行修改等操作，而是由DBMS系统自行维护。
选择一项：
对
错
正确答案是“对”。

04-19 、选中需要修改的表，然后在下拉菜单中选中“打开”即可修改表结构。
选择一项：
对
错
正确答案是“错”。



05-2 、在MySQL中字符串连接函数是________。
选择一项：
a.LOCATE
b.CONCAT
c.TRIM
d.SUBSTRING
正确答案是：CONCAT

05-7 、SQL的运算符主要有哪些？
选择一项：
a.算术运算符、赋值运算符、位运算符、线性运算符、逻辑运算符、一元运算符等。
b.算术运算符、赋值运算符、位运算符、比较运算符、线性运算符、一元运算符等。
c.算术运算符、赋值运算符、位运算符、比较运算符、集合运算符、一元运算符等。
d.算术运算符、赋值运算符、位运算符、比较运算符、逻辑运算符、一元运算符等。
正确答案是：算术运算符、赋值运算符、位运算符、比较运算符、逻辑运算符、一元运算符等。

05-8 、在MySQL数据库中，要访问任何一个对象都要通过其名称来完成，在SQL语言中，对数据库、表、变量、存储过程、函数等的定义和引用都需要通过_______来完成。
选择一项：
a.标识符
b.对象名
c.标示名
d.变量名
正确答案是：标识符

05-9、建立数据表，使用的SQL命令是（ ）
选择一项：
a.delete table
b.create database
c.new table
d.create table
正确答案是：create table

05-11 、查看数据库系统中已经存在的数据库时，可以执行（ ）命令
选择一项：
a.show database
b.show create database
c.show all database
d.show databases
正确答案是：show databases

05-17 、下面关于日期和时间函数说法
的是：
选择一项或多项：
a.DAY（CURDATE()）书写错误，不能正确返回值。
b.MONTH（'2018-12-28'）返回值为：12
c.CURDATE()函数可以返回当前系统的日期
d.YEAR（'2018-12-28'）返回值为：2018
正确答案是：
	CURDATE()函数可以返回当前系统的日期
	YEAR（'2018-12-28'）返回值为：2018
	MONTH（'2018-12-28'）返回值为：12

05-18 、SQL语言的核心语句是（   ）。
选择一项或多项：
a.数据操纵语句
b.数据查询语句
c.数据定义语句
d.数据控制语句
正确答案是：ABCD

05-19 、变量是指在程序运行过程中，值可以发生变化的量。在MySQL中可以使用变量类型（ ）。
选择一项或多项：
a.用户变量
b.局部变量
c.会话变量
d.全局变量
正确答案是：ABCD

05-20 、SQL的变量有哪些？
选择一项或多项：
a.会话变量
b.用户变量
c.全局变量
d.局部变量
正确答案是：ABCD

05-13 、要删除一个表及其结构使用DELETE命令。
选择一项：
对
错
正确答案是“错”。

05-14 、在创建表之前一定要用'USE DATABASE ……'命令指定使用的数据库。
选择一项：
对
错
正确答案是“对”。

05-15 、SHOW CREATE TABLE……命令可以查看指定表的创建命令，字符集，存储引擎等信息。
选择一项：
对
错
正确答案是“对”。

05-16 、SHOW DATABASE;命令正确，可以查看系统中存在的数据库名。
选择一项：
对
错
正确答案是“错”。



06-1 、数据库中由关系 students（学号，姓名，性别）,学号为主键。下面的括号应填入 DELETE FROM students _____________ 学号= '2018001';
选择一项：
a. UPDATE
b. DELETE
c. SELECT
d. WHERE
正确答案是：WHERE

06-2 、数据库中由关系 students（学号，姓名，性别）,学号为主键。下面的括号应填入 （ ） students SET 性别= '男';
选择一项：
a. UPDATE 
b. SELECT
c. ADD
d. DELETE
正确答案是：UPDATE

06-3 、数据库中由关系 students（学号，姓名，性别）,学号为主键。下面的括号应填入 DELETE ____________students SET 性别= '男';
选择一项：
a. UPDATE
b. FROM 
c. WHERE
d. SELECT
正确答案是：FROM

06-4 、SQL语言中，删除记录使用（ ）命令
选择一项：
a. create
b. insert
c. delete 
d. trcate
正确答案是：delete

06-5 、DELETE语句如果不添件_____________子句，将会把指定表的记录删除，但是表结构保留。
选择一项：
a. WHERE 
b. FROM
c. SELECT
d. JOIN
正确答案是：WHERE

06-18 、当我们在插入语句中省略某些列时，以下这些列是不可以省略的：
选择一项或多项：
a. 有外键约束字段
b. 非空约束但没有定义默认值的字段 
c. 非空约束字段
d. 主键字段
正确答案是：
	主键字段
	非空约束但没有定义默认值的字段

06-19 、当我们在插入语句中，以下这些列可以省略的：
选择一项或多项：
a. Timestamp类型（时间戳）的列 
b. 指定了默认值的列 
c. 允许为空的列 
d. 自动递增的列
正确答案是：ABCD

06-20 、关于DELETE语句的删除功能有：
选择一项或多项：
a. 删除表中一行或多行记录。 
b. 删除表结构。
c. 删除数据表中的数据记录。 
d. 删除表中某一行中的一个字段值。
正确答案是：
	删除数据表中的数据记录。
	删除表中一行或多行记录。

06-6 、SELECT 的功能是查询语句。
选择一项：
对 
错
正确答案是“对”。

06-7 、UPDATE 可以删除表中的记录。
选择一项：
对
错
正确答案是“错”。

06-9 、数据库中由关系 students（学号，姓名，性别，专业）。下面的语句可以执行新增操作： INSERT students（学号，姓名，性别，专业） VALUES ('2018001','孙悟空','男')。
选择一项：
对
错
正确答案是“错”。

06-10 、INSERT语句插入数据时，如果没有指定插入的字段名，则表示所有的字段都要插入数据。
选择一项：
对 
错
正确答案是“对”。

06-12 、数据库中由关系 students（学号，姓名，性别，专业）。下面的语句可以
的执行新增： INSERT students VALUES ('2018001','孙悟空','男')。
选择一项：
对
错
正确答案是“错”。

06-17 、使用UPDATE语句修改数据时，不能修改具有唯一约束的字段。
选择一项：
对
错
正确答案是“错”。



7. SQL语言中，插入记录使用（ ）命令
选择一项：
a. update
b. delete
c. insert 
d. select
正确答案是：insert

7. SQL语言中，修改 记录使用（ ）命令
选择一项：
a. select
b. delete
c. update 
d. insert
正确答案是：update

07-3 、SQL语言中，查询 记录使用（ ）命令
选择一项：
a. select 
b. insert
c. delete
d. where
正确答案是：select

07-4 、在SQL的查询语句中，ORDER BY选项实现对结果表的（ ）功能
选择一项：
a. 汇总
b. 排序 
c. 计算
d. 统计
正确答案是：排序

07-5 、模糊查询的运算符是：
选择一项：
a. like 
b. as
c. to
d. where
正确答案是：like

07-6 、数据库中有 students表，字段如下： sno(学号-主键)，sname(学生姓名-非空)，xb(性别-非空)，zhy(专业-非空)，in_year(入学年份)，dept(所在系部) 。 sc表，字段如下： sno(学号-主键)，cno(课程号-非空)，cj(成绩-非空)，xq(学期-非空) 。 courses表，字段如下：cno(课程号-主键), cname(课程名称-非空), xf(学分-非空), tno(任课教师号)。 teachers表，字段如下：tno (教师号-主键), tname(教师姓名-非空), txb(教师性别-非空), zc(职称), age(年龄)。 要查询已经选修课程的学生学号、学生姓名、课程号、成绩，应该使用（ ）。
选择一项：
a. sc表和students表的交叉连接查询。
b. sc表和course表的交叉连接查询。
c. sc表和courses表的自然连接查询。
d. sc表和students表的自然连接查询。
正确答案是：sc表和students表的自然连接查询。

07-7 、数据库中有 students表，字段如下：
sno(学号-主键)，sname(学生姓名-非空)，xb(性别-非空)，zhy(专业-非空)，in_year(入学年份)，dept(所在系部) 。
sc表，字段如下：
sno(学号-主键)，cno(课程号-非空)，cj(成绩-非空)，xq(学期-非空) 。
courses表，字段如下：
cno(课程号-主键), cname(课程名称-非空), xf(学分-非空), tno(任课教师号)。
teachers表，字段如下：
tno (教师号-主键), tname(教师姓名-非空), txb(教师性别-非空), zc(职称), age(年龄)。
要查询已经选修课程的学生学号、学生姓名、课程号、成绩，查询语句是（ ）。
选择一项：
a. SELECT a.sno,sname,cno,cj FROM sc a JOIN sourses b ON a.cno=b.sno
b. SELECT a.sno,sname,cno,cj FROM students a JOIN sc b ON a.sno=b.sno 
c. SELECT a.sno,sname,cno,cj FROM sc a CROSS JOIN courses b ON a.cno
d. SELECT a.sno,sname,cno,cj FROM students a CROSS JOIN sc b ON a.sno=b.sno
正确答案是：SELECT a.sno,sname,cno,cj FROM students a JOIN sc b ON a.sno=b.sno

07-8 、数据库中有 students表，字段如下： sno(学号-主键)，sname(学生姓名-非空)，xb(性别-非空)，zhy(专业-非空)，in_year(入学年份)，dept(所在系部) 。 teachers表，字段如下：tno (教师工号-主键), tname(教师姓名-非空), txb(教师性别-非空), zc(职称), age(年龄)，leader（上级领导的教师号）。 需要查询每个老师的工号、姓名、职称、上级领导的姓名，应该进行（ ）。 
选择一项：
a. teachers表的自查询。 
b. teachers表和students表的自然连接查询。
c. students表和teachers表的自然连接查询。
d. students表和teachers表的全外连接查询。
正确答案是：teachers表的自查询。

07-9 、在SQL语言中，SUM作用是（ ）
选择一项：
a. 个数
b. 平均值
c. 最大值
d. 求和
正确答案是：求和

07-10 、在SQL语言中，MAX作用是（ ）
选择一项：
a. 平均值
b. 最小值
c. 最大值 
d. 和
正确答案是：最大值

07-11 、在SQL语言中，% 通配（ ）个字符
选择一项：
a. 一个
b. 两个
c. 十个
d. 任意多个
正确答案是：任意多个

07-12 、在SQL语言中，_ 通配（ ）个字符
选择一项：
a. 任意多个
b. 一个 
c. 两个
d. 十人
正确答案是：一个

07-13 、查询students表每个学生的年级值，其代码如下：
SELECT sno AS 学号,sname AS 姓名,( ) AS 年级 FROM students ;
选择一项：
a. YEAR(NOW())-in_year
b. YEAR(NOW())-in_year+IF(MONTH(NOW())<9,0,1) 
c. NOW()-in_year
d. YEAR(NOW())-in_year+1
正确答案是：YEAR(NOW())-in_year+IF(MONTH(NOW())<9,0,1)

07-14 、数据库中有 students表，字段如下： sno(学号-主键)，sname(学生姓名-非空)，xb(性别-非空)，zhy(专业-非空)，in_year(入学年份)，dept(所在系部) 。 sc表，字段如下： sno(学号-主键)，cno(课程号-非空)，cj(成绩-非空)，xq(学期-非空) 。 courses表，字段如下：cno(课程号-主键), cname(课程名称-非空), xf(学分-非空), tno(任课教师号)。 teachers表，字段如下：tno (教师号-主键), tname(教师姓名-非空), txb(教师性别-非空), zc(职称), age(年龄)。 要查询所有学生与所有课程匹配后的结果列表，显示学生学号、学生姓名、 课程号、课程名称，应该使用（ ）。
选择一项：
a. courses表和students表的自然连接查询。
b. sc表和students表的交叉连接查询。
c. sc表和students表的自然连接查询
d. courses表和students表的交叉连接查询。
正确答案是：courses表和students表的交叉连接查询。

07-15 、查询teachers表中tno(教工号)、tname(姓名)和dept(所在部门)三个列，其代码如下： SELECT__________FROM teachers; 括号中的内容是？
选择一项：
a. *
b. tno，tname，dept 
c. tno、tname、dept
d. tno，tname，dept，
正确答案是：tno，tname，dept

07-16 、查询students表中sno(学号)、sname(姓名)和dept(所在院系)三个列，其列名用对应的中文名字显示，其代码如下：
SELECT（ ）FROM students;
括号中的内容是？
选择一项：
a. 以上都不对
b. sno as学号,sname as姓名,dept as所在系 
c. *
d. sno，sname，dept，
正确答案是：sno as学号,sname as姓名,dept as所在系

07-17 、WHERE cj ____________ 80 AND 89; WHERE cj>= 80 ANDcj<= 89; 两种描述方式效果一样。
选择一项：
a. GROUP
b. BETWEEN 
c. JOIN
d. AS
正确答案是：BETWEEN

07-18 、数据库中有
students表，字段如下： sno(学号-主键)，sname(学生姓名-非空)，xb(性别-非空)，zhy(专业-非空)，in_year(入学年份)，dept(所在系部) 。
teachers表，字段如下：tno (教师号-主键), tname(教师姓名-非空), txb(教师性别-非空), zc(职称), age(年龄)。
下面的查询结果是以上两个表的笛卡尔乘积的是：
选择一项：
a. students表和teachers表的交叉连接 
b. students表和teachers表的等值连接。
c. students表和teachers表内连接。
d. students表和teachers表的全外连接。
正确答案是：students表和teachers表的交叉连接

07-19 、HAVING子句必须配合GROUP BY子句一起出现。
选择一项：
对 
错
正确答案是“对”。

07-20 、某些查询既可以使用连接查询也可以使用子查询得到结果。
选择一项：
对 
错
正确答案是“对”。

07-21 、HAVING子句的效果与WHERE子句类似，某些情况下二者可以互换。
选择一项：
对
错
正确答案是“错”。

07-22 、select * from students; 表示查询students表所有字段。
选择一项：
对 
错
正确答案是“对”。

07-23 、模糊查询的通配符"%”和"_”作用一样。
选择一项：
对
错
正确答案是“错”。

07-24 、分组查询使用GROUP BY子句，或者ORDER BY子句。
选择一项：
对
错
正确答案是“错”。

07-25 、ORDER BY DESC 表示将查询结果进行降序排列。
选择一项：
对 
错
正确答案是“对”。

07-26 、“学生表 JOIN 成绩表 ON 学生表.课程号= 成绩表.课程号 ”该子句表示将学生表和成绩表进行等值连接查询。
选择一项：
对 
错
正确答案是“对”。

07-27 、“学生表 a JOIN 成绩表 b ON a.课程号= b.课程号”b表示成绩表的别名。
选择一项：
对 
错
正确答案是“对”。

07-28 、分组查询一定要配合统计函数进行。
选择一项：
对 
错
正确答案是“对”。

07-29 、在查询语句中的FROM子句，可以为需要查询的表起别名。
选择一项：
对 
错
正确答案是“对”。

07-30 、“SELECT MIN(*) FROM students;”可以查询学号最小的学生的信息。
选择一项：
对
错
正确答案是“错”。

07-31 、COUNT 函授是统计记录个数。
选择一项：
对 
错
正确答案是“对”。

07-32 、HAVING子句用于在分组查询中限定统计结果的筛选条件。
选择一项：
对 
错
正确答案是“对”。

07-33 、模糊查询的通配符有：
选择一项或多项：
a. like
b. _ 
c. % 
d. *
正确答案是：
	%
	_

07-34 、下面关于连接查询的说法，
的是：
选择一项或多项：
a. 自然连接是等值连接的一种，等值连接又是内连接的一种。 
b. 两个表的左外连接和右外连接都不是交叉连接的子集。 
c. 内连接查询，返回的结果集是两个表中所有相匹配的数据，而舍弃不匹配的数据。 
d. 自连接就是自然连接。
正确答案是：
	自然连接是等值连接的一种，等值连接又是内连接的一种。
	内连接查询，返回的结果集是两个表中所有相匹配的数据，而舍弃不匹配的数据。
	两个表的左外连接和右外连接都不是交叉连接的子集。

07-35 、数据库中有
students表，字段如下：
sno(学号-主键)，sname(学生姓名-非空)，xb(性别-非空)，zhy(专业-非空)，in_year(入学年份)，dept(所在系部) 。
sc表，字段如下：
sno(学号-主键)，cno(课程号-非空)，cj(成绩-非空)，xq(学期-非空) 。
courses表，字段如下：
cno(课程号-主键), cname(课程名称-非空), xf(学分-非空), tno(任课教师号)。
teachers表，字段如下：
tno (教师号-主键), tname(教师姓名-非空), txb(教师性别-非空), zc(职称), age(年龄)。
要查询所有学生学号、姓名、所选课程号、成绩，如果学生尚未选课也应列出学号和姓名，所选课程号、课程名称、成绩显示为空，则应该使用（ ）。
选择一项或多项：
a. students表和sc表的左外连接查询。 
b. students表和sc表的自然连接查询。
c. 查询语句为：SELECT a.sno,sname,cno,cj FROM students a LEFT JOIN sc b ON a.sno=b.sno; 
d. 查询语句为：SELECT a.sno,sname,cno,cj FROM students a JOIN sc b ON a.sno=b.sno;
正确答案是：
	students表和sc表的左外连接查询。
	查询语句为：SELECT a.sno,sname,cno,cj FROM students a LEFT JOIN sc b ON a.sno=b.sno;



10-1 、索引(index)是赖________建立的,它的作用是用来提高数据的_________速度。
选择一项：
a. 索引表,处理
b. 索引表,处理
c. 数据表,查询 
d. 数据表,处理依
正确答案是：数据表,查询

10-2 、每个表上最多可以创建_______个聚集索引,_________个非聚集索引。
选择一项：
a. 1,多 
b. 多,多
c. 1,1
d. 多,1
正确答案是：1,多

10-5 、视图是由查询语句构成的,是基于选择查询的___________。因而建立视图时最重要的内容时构成视图的_____________。
选择一项：
a. 基本表,查询语句
b. 虚拟表,基本表
c. 虚拟表,查询语句 
d. 基本表,虚拟表
正确答案是：虚拟表,查询语句

10-6 、索引是在基本表的列上建立的一种数据库对象，它同基本表分开存储，使用它能够加快数据的（ ）速度
选择一项：
a. 删除
b. 查询 
c. 插入
d. 修改
正确答案是：查询

10-7 、数据库中的数据保存在（ ）中
选择一项：
a. 索引
b. 数据表 
c. 视图
d. 存储过程
正确答案是：数据表

10-8 、以下哪种情况应尽量创建索引（ ）。
选择一项：
a. 需要更新频繁的基本表
b. 记录较少的基本表
c. 具有很多NULL值的列
d. 在Where子句中出现频率较高的列
正确答案是：在Where子句中出现频率较高的列

10-9 、唯一索引也称为__________。
选择一项：
a. 唯一连接
b. 唯一查询
c. 唯一索引 
d. 唯一视图
正确答案是：唯一索引

10-30 、在创建表窗口对“课程号”、“学生姓名”两个字段都选择了主键标示则表示创建了__________。
选择一项或多项：
a. 操作错误
b. 复合索引作为主键 
c. 一个主键 
d. 两个主键
正确答案是：一个主键, 复合索引作为主键

10-25 、下面关于聚集索引和非聚集索引的说法
的是:
选择一项或多项：
a. 非聚集索引查询时先从索引页上获取记录位置,再找到所需要的记录内容。 
b. 聚集索引改变数据表中记录的物理存储顺序,使之与索引列的顺序完全相同。 
c. 聚集索引查询时先从索引页上获取记录位置,再找到所需要的记录内容。
d. 非聚集索引不改变数据表中记录的存放顺序,只是将索引建立在索引页上。
正确答案是：聚集索引改变数据表中记录的物理存储顺序,使之与索引列的顺序完全相同。, 非聚集索引不改变数据表中记录的存放顺序,只是将索引建立在索引页上。, 非聚集索引查询时先从索引页上获取记录位置,再找到所需要的记录内容。

10-26 、在数据库中建立视图的好处有:
选择一项或多项：
a. 集中数据,简化查询操作:当用户多次使用同一个查询操作,而且数据来自于数据库中不同的表时,我们可以先建立视图再从视图中读取数据,以达到数据的集中管理和简化重复写查询命令的目的。 
b. 控制用户提取的数据,达到数据安全保护的目的:在数据库中不同的用户对数据的操作和查看范围往往是不同的,数据库管理人员通常为不同的用户设计不同的视图,使得数据库中的数据安全有保证。 
c. 便于数据的交换操作:当与其它类型的数据库交换数据(导入导出)时,如果原始数据存放在多个表中进行数据交换就比较麻烦。如果将要交换的数据集中到一个视图中,再进行交换就大大简化了交换操作。 
d. 提供指针指向表中指定字段的数据值,然后根据指定的排序次序来排列这些指针,通过查询索引找到特定的值,从而快速找到所需要的记录。
正确答案是：集中数据,简化查询操作:当用户多次使用同一个查询操作,而且数据来自于数据库中不同的表时,我们可以先建立视图再从视图中读取数据,以达到数据的集中管理和简化重复写查询命令的目的。, 控制用户提取的数据,达到数据安全保护的目的:在数据库中不同的用户对数据的操作和查看范围往往是不同的,数据库管理人员通常为不同的用户设计不同的视图,使得数据库中的数据安全有保证。, 便于数据的交换操作:当与其它类型的数据库交换数据(导入导出)时,如果原始数据存放在多个表中进行数据交换就比较麻烦。如果将要交换的数据集中到一个视图中,再进行交换就大大简化了交换操作。

10-27 、在MySQL中,索引有()、空间索引等。
选择一项或多项：
a. 普通索引 
b. 组合索引 
c. 唯一索引 
d. 主键索引
正确答案是：普通索引, 主键索引, 唯一索引, 组合索引

10-28 、以下关于视图的说法
的是:
选择一项或多项：
a. 视图可以简化数据操作 
b. 视图可以提高数据的安全性 
c. 视图是由查询构成的 
d. 视图是虚拟表
正确答案是：视图是由查询构成的, 视图是虚拟表, 视图可以提高数据的安全性, 视图可以简化数据操作

10-29 、下面哪些命令可以查看students表中的索引 
选择一项或多项：
a. SHOW INDEX FROM表名; 
b. CREATE INDEX索引名 ON表名;
c. ALTER表名 ADD PRIMARY KEY ON (字段名);
d. SHOW CREATE TABLE表名;
正确答案是：SHOW INDEX FROM表名;, SHOW CREATE TABLE表名;

10-14 、要提高数据库的查询速度，应该尽可能多的建立索引。
选择一项：
对
错
正确答案是“错”。

10-18 、数据库中只存储视图的定义,视图不生成所选数据行和列的永久拷贝。
选择一项：
对 
错
正确答案是“对”。

10-20 、建立索引的目的是提高增删查改等操作的效率。
选择一项：
对
错
正确答案是“错”。

10-21 、索引的使用是根据查询的需要由系统自动选择调用的,不需要用户的参与。
选择一项：
对 
错
正确答案是“对”。

10-23 、聚集索引是将数据表中的记录的物理存储地址按照索引字段的顺序排列，所以具有聚集索引的表在运行INSERT语句时需要的时间，比未建立聚集索引的表需要的时间长。
选择一项：
对 
错
正确答案是“对”。



13-7 、用于进行数据库备份的命令是：
选择一项：
a. 'mysqldump -uroot -p123456 students>C:/beifen20190328.sql' 
b. 'mysqldump -uroot -p123456 '
c. 'mysqldump -uroot -p123456 students
d. 'mysql -uroot -p123456 students'
正确答案是：'mysqldump -uroot -p123456 students>C:/beifen20190328.sql'

13-20 、当数据库损坏时，数据库管理员可通过（ ）方式恢复数据库
选择一项：
a. DELETE语句
b. 事务日志文件 
c. 联机帮助文件
d. 主数据文件
正确答案是：事务日志文件

13-8 、如下命令进行运行
 Mysqldump-rroot–p students_courses students>c: /students_courses_2016.sql 下面的说法
的是：
选择一项或多项：
a. 这条命令是针对指定的整个数据库的操作。
b. 这是一条数据库恢复命令。
c. 这是一条数据库备份命令。 
d. 这条命令是针对指定的数据表的操作。
正确答案是：
	这是一条数据库备份命令。
	这条命令是针对指定的数据表的操作。

13-9 、冷备份的优点是：
选择一项或多项：
a. 容易归档（简单拷贝即可） 
b. 简单快速（只需拷文件） 
c. 低度维护，高度安全 
d. 容易恢复到某个时间点上（只需将文件再拷贝回去）
正确答案是：ABCD

13-10 、冷备份的不足是：
选择一项或多项：
a. 单独使用时，只能提供到"某一时间点上”的恢复。 
b. 在实施备份的全过程中，数据库不能开展其他工作。也就是说，在冷备份过程中，数据库必须是关闭状态。 
c. 可以按表或按用户恢复。
d. 若磁盘空间有限，只能拷贝到磁带等其他外部存储设备上，速度会很慢。
正确答案是：
	单独使用时，只能提供到"某一时间点上”的恢复。
	在实施备份的全过程中，数据库不能开展其他工作。也就是说，在冷备份过程中，数据库必须是关闭状态。
	若磁盘空间有限，只能拷贝到磁带等其他外部存储设备上，速度会很慢。

13-11 、热备份的不足是：
选择一项或多项：
a. 难于维护，所以要特别仔细小心，不允许"以失败告终” 
b. 不能按表户恢复。
c. 若热备份不成功，所得结果不可用于时间点的恢复。 
d. 不能出错，否则后果严重。
正确答案是：
	不能出错，否则后果严重。
	若热备份不成功，所得结果不可用于时间点的恢复。
	难于维护，所以要特别仔细小心，不允许"以失败告终”

13-12、下面关于热备份的优点说法
的是：（）
选择一项或多项：
a. 以上全都正确。
b. 热备份的优点是备份的时间长，但可靠性高。
c. 热备份的优点是备份时数据库仍可使用。 
d. 热备份的优点是可达到秒级恢复（恢复到某一时间点上）。
正确答案是：
	热备份的优点是备份时数据库仍可使用。
	热备份的优点是可达到秒级恢复（恢复到某一时间点上）。

13-13 、关于"差分备份(differential backup)”说法
的是：~
选择一项或多项：
a. 每天对数据库系统的所有数据、对象、参数、日志等进行全面备份。
b. 差分备份策略在避免了完全备份和增量备份两种策略的缺陷的同时，又具有了它们的所有优点。 
c. 它无需每天都对系统做完全备份，因此备份所需时间短，并节省了磁盘空间，其次，它的灾难恢复也很方便。 
d. 对于那些业务繁忙、备份时间有限的单位来说，选择这种备份策略是不明智的。
正确答案是：
	差分备份策略在避免了完全备份和增量备份两种策略的缺陷的同时，又具有了它们的所有优点。
	它无需每天都对系统做完全备份，因此备份所需时间短，并节省了磁盘空间，其次，它的灾难恢复也很方便。

13-14 、关于"完全备份（full backup）”说法
的是：~
选择一项或多项：
a. 它亦有不足之处，首先，由于每天都对整个系统进行完全备份，造成备份的数据大量重复。这些重复的数据占用了大量的磁盘空间，这对用户来说就意味着增加成本。 
b. 由于需要备份的数据量较大，因此备份所需的时间也就较长。 
c. 完全备份（full backup）指在一次系统完全备份的基础上，在接下来的几天里，管理员再将当天所有与完全备份时不同的数据（新的或修改过的）备份到磁盘上。
d. 这种备份策略的好处是：当发生数据丢失灾难时，只要用灾难发生前一天的备份就可以恢复丢失的数据。
正确答案是：ABCD

13-15 、关于"增量备份(incremental backup)”说法
的是：~
选择一项或多项：
a. 这种备份策略的优点是节省了磁盘空间，缩短了备份时间。 
b. 在完全备份的基础上，只对当天新的或被修改过的数据进行备份。 
c. 这种备份的可靠性也很差。在这种备份方式下，各盘磁盘间的关系就象链子一样，一环套一环，其中任何一盘磁盘出了问题都会导致整条链子脱节。 
d. 它的缺点在于，当灾难发生时，数据的恢复比较麻烦。这种方式很繁琐。
正确答案是：ABCD

13-16 、根据数据库备份时是否运行，可以将数据库备份方式分为（ ）
选择一项或多项：
a. 离线备份
b. 在线备份
c. 冷备份 
d. 热备份
正确答案是：
	热备份
	冷备份

13-18 、下面的MYSQL数据库的还原命令说法
的是（ ）
选择一项或多项：
a. mysql 还原命令可以在参数-p后写上密码 
b. 还原命令是：mysqldump -uroot -p123456 students>D:/backup.sql
c. mysql 还原命令可以仅还原指定的数据库和指定的表 
d. 还原命令是：mysqldump -uroot -p123456 students
正确答案是：
	mysql 还原命令可以在参数-p后写上密码
	mysql 还原命令可以仅还原指定的数据库和指定的表

13-17 、被采用最多的数据库备份策略有（ ）
选择一项或多项：
a. 增量备份 
b. 不完全备份
c. 完全备份 
d. 差分备份
正确答案是：
	完全备份
	增量备份
	差分备份

13-3 、热备份是在数据库运行的情况下，备份数据库的方法。所以，如果你有昨天夜里的一个冷备份而且又有今天的热备份文件，在发生问题时，就可以利用这些资料恢复更多的信息。
选择一项：
对 
错
正确答案是“对”。

13-5 、mysqldump 是采用SQL级别的备份机制，它将数据表导成 SQL 脚本文件。
选择一项：
对 
错
正确答案是“对”。
